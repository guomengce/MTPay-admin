# MTPay 项目模块说明

> 本文件说明MTPay包含的业务模块，并记录当前开发进度和后续开发顺序。
> 进度说明用于帮助新开发对话快速定位，不替代真实代码、migration和用户的ApiPost接口测试结果。接口最终验证由用户完成，除非用户明确要求，不在 `tests/` 目录创建接口测试用例。
> 新开发对话开始后，必须先阅读 `AGENTS.md`、本文件和当前实际代码，不得根据本文推断未写明的数据库实现。

## 0. 当前开发进度（更新于2026-08-18）

### 第一阶段结论

第一阶段（第一版后端业务接口开发）已经完成。管理员、代理认证、代理账户、业务配置、资产总览、入金、兑换、白名单、USD出金、资产余额、统一交易记录、管理端运营总览和管理员操作记录均已有后端实现并完成当前约定范围的接口验证；统一交易记录已经从三表聚合升级为 `transaction_records` 投影表。原资金流水不再作为对外业务模块，管理端和代理端查询接口、Controller、Facade及总览返回已经移除，只保留事务内余额变动审计写入。已通过用户ApiPost测试的模块以当前实际代码和接口行为为准。

当前不再默认继续开发新的后端模块。模块16业务结果通知、统一审核中心、管理员登录安全扩展等能力继续暂缓；后续如开始新阶段，应由用户明确指定前端接入、联调修正或新的业务模块，不得把暂缓项自动视为下一模块。

### 已完成并通过ApiPost测试

#### 管理员基础模块

- 管理员独立登录；
- 管理员列表、详情、新增、修改、状态修改和删除接口；
- 管理员相关接口属于已验证的现有功能，后续模块默认不重构。

#### 代理登录与账户管理模块

模块2中的代理端认证接口和模块3中的代理账户管理接口已经完成，并已根据实际使用情况调整。当前代码为后续开发基线，不应在新模块中顺带重构。

管理后台已完成：

- 新增待激活代理；
- 自动生成代理编号；
- 代理列表、详情和资料修改；
- 正常、暂停、停用状态流转；
- 重新发送激活邀请；
- 发送密码重置邮件；
- 查看最近登录记录；
- 查看邀请和密码邮件的发送状态、次数及失败原因。

代理端已完成：

- 邀请链接激活并设置密码；
- 账号密码登录；
- 登录失败次数限制和临时锁定；
- 代理专用Token认证；
- 获取当前代理资料；
- 找回和重置密码；
- 登录后修改密码；
- 主动退出登录；
- 暂停、停用或修改密码后使旧Token失效；
- 阻止代理Token访问管理后台接口。

当前代理状态口径：

```text
0 待激活
1 正常
2 暂停，可恢复为正常
3 停用，终止状态，不通过普通状态接口恢复
```

相关路由：

```text
管理后台：routes/admin/admin.php
代理端：routes/web/web.php
```

#### 币种、网络和业务配置模块

模块5已经完成，全部接口已经由用户使用ApiPost测试通过。用户已根据实际使用情况调整部分代码，当前真实代码为后续业务模块的开发基线；除非发现明确问题或用户提出新需求，后续不重构本模块。

币种管理已完成：

- migration固定初始化USDT、USDC、USD；
- migration固定初始化TRC20、ERC20；
- 固定USDT/TRC20、USDT/ERC20、USDC/ERC20三组币种网络关系；
- 三组关系初始停用，配置真实平台收款地址后才允许启用；
- 客户只在“币种管理”中维护真实平台收款地址和币种网络关系启停，不提供币种、网络或关系的新增、修改、删除接口；
- 每个币种网络组合只允许一个当前有效地址；
- 更换地址时停用旧地址并保留完整历史。

第二阶段已完成：

- 配置USDT→USD、USDC→USD平台默认兑换比例；
- 为指定代理配置专属兑换比例；
- 代理专属比例优先于平台默认比例；
- 清除代理专属比例后恢复使用平台默认比例；
- 配置USD每笔固定出金手续费；
- 比例和手续费使用高精度decimal及十进制字符串处理，不使用PHP浮点数。

模块5共10个后台接口，统一位于 `routes/admin/admin.php`，并使用后台Token认证：

```text
GET  /admin/getCurrencyNetworkList
GET  /admin/getCurrencyNetworkInfo
POST /admin/editCurrencyNetworkStatus
POST /admin/setReceivingAddress
GET  /admin/getRateFeeConfig
POST /admin/setDefaultExchangeRates
GET  /admin/getAgentExchangeRateList
POST /admin/setAgentExchangeRates
POST /admin/clearAgentExchangeRates
POST /admin/setUsdWithdrawalFee
```

后续入金订单必须保存提交时的平台收款地址快照；兑换订单必须保存提交时实际使用的兑换比例；出金订单必须保存提交时的固定手续费。配置修改不能回写或影响历史订单。

#### 资产余额模块

模块10已经完成，全部接口已经由用户使用ApiPost测试通过。用户已根据实际使用情况调整部分代码，不影响整体流程；当前真实代码、migration和接口行为是后续资金模块的开发基线，不应重新生成或顺带重构。

- 每个代理固定维护USDT、USDC、USD三项资产；
- 每项资产分别记录可用余额和冻结余额，总余额在查询时计算；
- migration已为历史代理初始化三币种零余额；
- 新增代理时在创建代理的同一事务中初始化三币种余额；
- 代理端根据Token身份查询自己的资产，不接收前端代理ID；
- 管理后台支持按代理、关键字和状态分页查询资产；
- 已提供事务内增加、扣减、冻结、解冻及正式扣除冻结余额的内部能力；
- 金额使用 `decimal(28,8)`、BCMath和十进制字符串，不使用PHP浮点数；
- 后台未开放人工修改余额接口。

已确认的资金口径：

```text
入金：提交不变动余额，审核通过后增加可用余额
兑换：提交时冻结，审核通过后扣除冻结资产并增加USD，驳回时解冻
出金：提交时冻结出金金额和手续费，审核通过后继续冻结，实际付款完成后正式扣除
出金审核驳回或付款失败：冻结金额退回可用余额
```

相关接口：

```text
GET /web/getAgentAssets
GET /admin/getAgentAssetList
```

#### 代理资产总览、数字货币入金和数字货币兑换

模块4、模块6、模块7及内部余额变动审计已经完成开发，相关业务接口已经由用户使用ApiPost测试通过。用户之后根据实际情况调整了部分代码，但未改变整体业务流程；后续开发必须以当前真实代码、migration和接口行为为基线，不重新生成或顺带重构这些模块。

- 资产总览已聚合三币种余额、实际兑换比例、待处理数量及最近5笔入金/兑换/出金交易；`recent_orders` 已与模块12统一交易记录复用相同的格式化和排序口径；总览不再返回 `recent_fund_flows`；
- 代理端按Token身份查看自己的总览，后台按代理ID查看同口径总览；
- 当前本地USDT/TRC20、USDT/ERC20、USDC/ERC20三条虚假收款地址及通道均已启用；
- 入金提交只保存申请和当前地址快照，不做链上地址真实性或区块确认校验；
- 同一网络TxID永久唯一，已驳回订单的TxID也不能再次提交；
- 入金审核通过时增加对应数字货币可用余额并生成内部余额变动审计记录，驳回不变动余额；
- 兑换仅支持USDT、USDC兑换USD，代理专属比例优先并保存比例快照；
- 兑换提交时冻结来源币种，审核通过后扣除冻结余额并增加USD，驳回时解冻；
- USD结果使用BCMath并按2位小数四舍五入，结果为0.00时拒绝提交；
- 已有待审核订单可在代理后来暂停或停用后继续由后台处理；
- 入金金额不允许后台修改，所有订单只允许审核一次。

新增接口：

```text
GET  /web/getAgentAssetOverview
GET  /web/getDepositChannelList
POST /web/submitDeposit
GET  /web/getDepositList
GET  /web/getDepositInfo
GET  /web/getExchangeConfig
POST /web/submitExchange
GET  /web/getExchangeList
GET  /web/getExchangeInfo

GET  /admin/getAgentAssetOverview
GET  /admin/getDepositList
GET  /admin/getDepositInfo
POST /admin/reviewDeposit
GET  /admin/getExchangeList
GET  /admin/getExchangeInfo
POST /admin/reviewExchange
```

#### 内部余额变动审计（原模块11）

原模块11已经从对外业务模块收缩为后端内部审计能力。管理端和代理端的四个只读查询接口、Controller、Facade、筛选查询代码以及资产总览返回均已移除；`fund_flows` 表、Model、事务内写入服务和历史数据继续保留，作为入金、兑换、USD出金余额变化的审计基线。

- 一条审计记录对应一个币种账户的一次真实余额变化；
- 可用余额和冻结余额分别记账，冻结及解冻各生成两条账户流水；
- 保存业务事件、业务类型、业务编号、币种、账户类型、方向、金额及变动前后余额；
- `event_key + currency_id + account_type`唯一约束用于阻止重复入账、冻结、解冻或扣款；
- 余额变动和流水写入由业务Service在同一数据库事务中完成；
- 历史流水不开放新增、修改或删除接口，错误资金预留新增冲正流水方式处理；
- 待审核但尚未发生余额变化的业务申请不生成资金流水。

当前不提供任何管理端或代理端资金流水查询接口，前端只接入统一交易记录。

#### 付款人和收款人白名单模块

模块8的业务流程、双端原型、数据库表和后端接口已经完成，全部接口已由用户使用ApiPost测试通过。用户之后根据实际情况调整了部分代码，但不影响整体流程；当前真实代码、migration和接口行为是USD出金及后续业务的白名单基线，不重新生成或顺带重构。

- 支持付款人公司、付款人个人、收款人公司和收款人个人四类白名单；
- 白名单角色、主体类型、企业类型和证件类型使用固定数字编码；
- 国家/地区保存前端提交的文字，汇款目的保存前端维护的正整数编码；
- 使用代理Token专用单文件上传接口，文件保存到私有目录；
- 支持首次提交0至5个文件，以及每轮补交1至5个文件；
- 未绑定文件超过24小时后由定时任务清理，已绑定文件通过鉴权接口预览或下载；
- 支持待审核、待补充文件、已通过和已驳回状态，以及多轮补交和双方公开留言；
- 主资料、流程记录和证明文件分三张表保存，流程和已绑定文件不允许修改或删除；
- 后台审核和代理补交使用事务及行锁校验状态，防止并发重复处理；
- 只有属于当前代理且状态为已通过的付款人和收款人白名单，才能在USD出金中使用。

相关接口：

```text
POST /web/uploadWhitelistFile
POST /web/submitWhitelist
GET  /web/getWhitelistList
GET  /web/getWhitelistInfo
POST /web/supplementWhitelist
GET  /web/previewWhitelistFile
GET  /web/downloadWhitelistFile

GET  /admin/getWhitelistList
GET  /admin/getWhitelistInfo
POST /admin/requestWhitelistSupplement
POST /admin/reviewWhitelist
GET  /admin/previewWhitelistFile
GET  /admin/downloadWhitelistFile
```

#### USD出金模块

模块9的双端原型、数据库迁移、私有文件、订单和时间线模型、代理端与管理后台接口、资金冻结/解冻/正式扣款及临时文件清理均已完成，全部接口已经由用户使用ApiPost测试通过。用户之后根据实际情况调整了部分代码，但未改变整体流程；当前真实代码、migration和接口行为是后续开发基线，不重新生成或顺带重构。

- 第一版只支持USD出金，出金金额表示收款人实际收到金额；
- 提交时读取固定手续费并保存快照，总扣款为出金金额加手续费；
- 提交时保存付款人和收款人的完整白名单资料快照；
- 不区分同名公司、同一个人或其他组合，代理统一使用非必填“上传证明文件”入口；
- 首次提交允许0至5个证明文件，管理员可要求代理多轮补件，每轮补交1至5个文件；
- 审核通过后进入付款处理中并保持资金冻结，审核驳回立即解冻；
- 付款完成时正式扣除冻结资金，可选绑定0至5个付款凭证；
- 已完成订单允许管理员分轮追加付款凭证，代理可通过鉴权接口查看；
- 付款失败立即解冻并成为终态，原订单不能重试，代理必须重新提交新订单；
- 私有文件只允许PDF、PNG、JPG、JPEG，单个最大10MB，未绑定文件24小时后清理；
- 订单、时间线、私有文件及资金流水均通过事务、行锁和稳定事件键防止重复处理；
- 出金迁移 `2026_08_13_110000_create_withdrawal_tables` 已执行。

相关接口：

```text
GET  /web/getWithdrawalConfig
POST /web/uploadWithdrawalFile
POST /web/submitWithdrawal
GET  /web/getWithdrawalList
GET  /web/getWithdrawalInfo
POST /web/supplementWithdrawal
GET  /web/previewWithdrawalFile
GET  /web/downloadWithdrawalFile

GET  /admin/getWithdrawalList
GET  /admin/getWithdrawalInfo
POST /admin/requestWithdrawalSupplement
POST /admin/reviewWithdrawal
POST /admin/uploadWithdrawalFile
POST /admin/processWithdrawalPayment
POST /admin/appendWithdrawalPaymentFiles
GET  /admin/previewWithdrawalFile
GET  /admin/downloadWithdrawalFile

定时清理命令
php artisan withdrawal:cleanup-temp-files
```

#### 统一交易记录与详情模块

模块12已经完成。双端统一交易列表、统一详情及资产总览近期交易的接口契约此前已经由用户使用ApiPost测试通过；2026-08-18完成 `transaction_records` 投影表升级并由用户确认本次完善完成。当前真实代码和接口行为是后续前端接入及业务扩展的开发基线；除非发现明确问题或用户提出新需求，不重新生成或顺带重构。

- 统一交易记录只包含数字货币入金、数字货币兑换和USD出金，一笔业务订单对应一条交易记录；
- 交易记录与内部余额审计保持独立：交易记录反映业务订单，内部审计记录账户余额的实际变化；
- 已新增统一 `transaction_records` 投影表，替代原先对三类订单执行 `UNION ALL` 的列表查询；
- 入金、兑换和USD出金订单提交时在原业务事务内创建一条投影，后续状态变化在同一事务内同步；
- 原业务订单继续作为业务事实来源，投影表只服务统一列表、搜索、分页、近期交易和后续统计；
- 统一返回结构化、无符号金额字段及统一状态分组，由前端根据交易类型决定展示文案和形式，不允许前端重新计算真实金额；
- 统一详情接口按交易类型复用现有三类业务详情逻辑，原有业务详情接口继续保留；
- 代理端只能查询当前Token所属代理的交易，管理端可以查询全平台交易并按代理筛选；
- 代理资产总览 `recent_orders` 已复用统一交易格式；双端总览只展示近期交易，对外资金流水接口已经移除，第一版双端CSV导出入口均隐藏；
- 投影表升级后保持参数和返回结构不变的接口：

```text
GET /web/getTransactionList
GET /web/getTransactionInfo
GET /admin/getTransactionList
GET /admin/getTransactionInfo
```

#### 管理端运营总览

管理端运营总览聚合接口已于2026-08-18补齐，并已完成本地语法、路由、真实数据库返回验证及用户ApiPost测试。当前接口结构和统计口径作为后续前端接入基线，不再退回由前端分别请求列表并自行汇总的实现。

- 新增 `GET /admin/getOperationOverview`，无请求参数，使用管理端Token；
- 一次返回正常代理统计、全平台代理三币种账面余额、四类待处理业务、近7日交易笔数趋势和最近5笔交易；
- 代理总数和余额只统计当前未删除代理；余额包含待激活、正常、暂停和停用代理；
- 余额按USDT、USDC、USD固定顺序返回可用、冻结、总额和冻结占比，真实金额始终为十进制字符串；
- 待处理入金、兑换和出金直接统计 `transaction_records`，白名单统计原白名单表；
- 近7日趋势包含今天，按订单提交时间及业务类型统计笔数，无交易日期补0；
- 最近交易固定最多5笔，与统一交易列表复用同一投影、格式化和稳定排序口径；
- 总览只提供查看信息，不提供任何审核、补件、付款或追加凭证操作。

相关实现：

```text
app/Http/Controllers/Admin/OperationOverviewController.php
app/Facades/Admin/OperationOverviewFacade.php
app/Services/Admin/OperationOverviewService.php
app/Models/AgentBalances.php
app/Services/Agent/TransactionService.php
```

### 已部分完成的公共能力

- 模块13“审核中心”：入金、兑换、白名单和USD出金的后台列表、详情、审核及状态防重已经随业务完成；当前按业务分别处理，尚未开发额外的统一审核中心聚合接口。
- 模块14“处理时间线”：白名单和USD出金保存独立流程记录；入金和兑换通过订单状态、处理时间和内部余额变动审计追溯。第一版不额外建立所有业务共用的时间线表。
- 模块15“文件与附件”：白名单证明文件、出金证明文件和付款凭证的私有上传、绑定、预览、下载、权限隔离及24小时临时清理已经完成；第一版按业务独立存储，不另建全局附件中心。
- 模块16“邮件与通知”：已完成代理邀请、重新发送邀请、密码重置和登录Email变更通知；入金、兑换、白名单和USD出金业务结果通知尚未开发，用户已确认暂缓。
- 模块18“操作日志与安全”：现有代理登录安全能力保持不变；简单管理员操作记录的后端代码、migration、管理端列表接口及重要操作写入已经完成，并作为第一阶段收尾能力纳入当前开发基线。
- Token身份隔离、安全令牌和邮件发送记录已经完成，可供后续业务复用。

### 尚未开发或暂缓的能力

- 模块16入金、兑换、白名单和USD出金业务结果邮件通知暂缓，本轮及第一版接口收尾阶段不开发；
- 统一审核中心聚合接口仅作为可选扩展，当前暂缓，是否开发需由用户后续另行确认。

### 原型与接口核对结论

2026-08-14已按当前真实路由核对新版管理端和代理端原型：

- 管理端“币种管理”已经并入 `原型图/MTPay 管理后台 - 新版原型.html`，覆盖固定币种网络关系列表、详情、启停、设置或更换收款地址及地址历史；相关页面名称统一使用“币种管理”；
- 代理端入金页改为直接选择后台实际启用的币种网络关系，不再允许前端自由组合出后端不支持的 `USDC/TRC20`；
- 代理端和管理端均不提供“资金流水”入口；后端资金流水列表和详情接口已经移除，只保留内部余额变动审计数据；
- 代理端业务总览的近期交易改为整行宽度的自适应摘要列表，完整展示时间、类型、编号、内容、金额、状态和详情入口，不需要横向移动；
- 管理端代理账户页补齐代理详情、资产概况、状态流转、重发邀请和发起密码重置入口；代理端账户页补齐登录后修改密码入口；
- “比例与费用”页补齐清除代理专属比例并恢复平台默认值的入口；
- 双端总览只展示近期交易，不返回 `recent_fund_flows`；对外资金流水接口已删除，内部审计数据不删除；
- 管理端运营总览已提供单一聚合接口，页面不再分别拉取交易分页、余额和业务列表自行统计；
- 双端CSV导出入口继续隐藏，第一版不开发CSV导出接口；
- 入金、兑换、白名单、USD出金、交易记录、操作记录以及比例和费用的主要查询与操作均已有原型入口，与现有业务接口流程一致；
- 管理员账号CRUD、代理激活/找回密码/重置密码的邮件令牌落地页属于已有基础或辅助流程，目前未展开为新版业务原型中的独立页面；开始正式前端接入前可按实际产品入口补齐，但这不代表对应后端接口未完成。

### 下一阶段

第一阶段已经结束，目前没有默认的下一后端模块。后续建议从用户明确指定的范围开始：

```text
第一阶段后端接口完成
→ 新版原型作为前端接入基线
→ 用户指定前端开发、接口联调修正或新业务模块
```

## 1. 项目概述

MTPay是一个提供给代理企业使用的资金管理平台。

系统主要包括：

- 代理端；
- 管理后台；
- 后端业务接口；
- 资金与交易处理；
- 文件、通知和审计等基础能力。

代理可以向平台存入USDT或USDC，将数字货币兑换成USD，维护付款人和收款人白名单，并使用USD发起出金。

## 2. 登录与账户模块

> 当前进度：代理端认证接口已完成并通过ApiPost测试；管理员登录属于已完成的现有功能。页面刷新和停留位置属于前端接入行为，不代表后端还需重新开发登录接口。

### 代理端登录

需要实现：

- 代理使用账号和密码登录；
- 判断账号是否正常、暂停或停用；
- 登录失败次数限制和临时锁定；
- 修改密码和找回密码；
- 主动退出登录；
- 页面刷新后保持登录状态；
- 刷新页面只重新获取数据，不自动跳回登录页；
- 代理只能进入代理端，不能进入管理后台。

### 管理后台登录

需要实现：

- 管理员独立登录管理后台；
- 判断管理员账号是否启用；
- 登录失败次数限制和临时锁定；
- 修改密码和退出登录；
- 页面刷新后保持登录状态；
- 管理员不能通过代理端入口登录。

第一版管理员不需要角色选择和复杂权限分组。

## 3. 代理账户管理模块

> 当前进度：本模块后端接口已全部完成并通过ApiPost测试。除非后续发现明确缺陷或用户提出新需求，不再重构。

管理后台需要实现：

- 新增代理账号；
- 填写企业名称、Email和电话；
- 查看代理列表；
- 查看代理详情；
- 修改代理信息；
- 启用、暂停或停用代理；
- 重置代理密码；
- 查看代理最近登录信息；
- 根据实际方案发送账号邀请或激活邮件；
- 重新发送邀请；
- 查看邀请发送成功或失败状态。

新增代理暂时不需要企业审批流程。

## 4. 代理资产总览模块

> 当前进度：后端代码、接口和聚合查询已完成并通过用户ApiPost测试。用户后续按实际情况所做的代码调整不影响整体流程，当前真实代码为后续开发基线。现有 `recent_orders` 返回当前代理最近5笔入金、兑换和USD出金交易，并已与模块12统一交易记录复用相同口径；`recent_fund_flows` 已从响应中移除。管理端运营总览通过 `GET /admin/getOperationOverview` 一次返回全平台近期交易及其他总览统计，该接口已通过用户ApiPost测试。

代理端需要展示：

- USDT余额；
- USDC余额；
- USD余额；
- 可用余额；
- 如采用资金冻结机制，则展示冻结余额；
- 待审核业务数量；
- 最近交易；
- 最近资金变动；
- 常用业务入口。

管理后台需要能够查看每个代理的资产概况和相关资金记录。

## 5. 币种、网络和业务配置模块

> 当前进度：模块后端代码已完成，10个接口已经全部通过用户ApiPost测试。用户后续根据实际情况所做的代码调整不影响整体流程，当前真实代码为后续模块基线。

### 币种管理：固定币种网络关系和平台收款地址

第一版固定币种：

```text
USDT
USDC
USD
```

第一版固定区块链网络：

```text
TRC20
ERC20
```

第一版固定支持矩阵：

```text
USDT / TRC20
USDT / ERC20
USDC / ERC20
```

币种、网络和三条固定关系由migration初始化，不向客户提供新增、修改或删除入口。管理页面统一命名为“币种管理”，客户只需要：

- 为三条固定关系填写真实平台收款地址；
- 启用或停用每条币种网络关系；
- 查看关系详情和地址历史；
- 更换地址时保留旧地址历史。

关键规则：

- 三条固定关系初始化为停用；
- 没有当前有效平台收款地址时不能启用关系；
- 每个币种网络组合只能有一个当前有效地址；
- 换址在事务中完成，旧地址停用但不删除；
- 停用币种网络关系只影响新入金，不能改变历史订单。

### 第二阶段：兑换比例和出金手续费

已经实现：

- USDT→USD平台默认兑换比例；
- USDC→USD平台默认兑换比例；
- 代理专属兑换比例；
- 清除代理专属比例并恢复默认值；
- USD每笔固定出金手续费。

关键规则：

- 代理专属比例优先于平台默认比例；
- 兑换比例必须大于0；
- USD固定出金手续费允许为0；
- 比例和手续费使用高精度decimal和十进制字符串；
- 配置修改只影响新申请；
- 兑换订单和出金订单必须保存提交时使用的比例、手续费快照。

### 本模块明确不做

- 不提供币种、网络或币种网络关系的后台CRUD；
- 不实现系统字典模块；企业类型、汇款目的、证件类型、国家/地区等内容由用户与前端人员对齐；
- 不在本模块创建兑换订单、出金订单或资产余额；对应业务在后续模块实现。

主要代码位置：

```text
routes/admin/admin.php
app/Http/Controllers/Admin/AssetConfigController.php
app/Facades/Admin/AssetConfigFacade.php
app/Services/Admin/AssetConfigService.php
app/Models/
database/migrations/
```

## 6. 数字货币入金模块

> 当前进度：后端代码、订单表、审核入账和资金流水已完成并通过用户ApiPost测试。用户后续按实际情况所做的代码调整不影响整体流程，当前真实代码为后续开发基线。配置依赖模块5，只读取当前实际生效的关系与地址，并保存提交时地址快照。

第一版只支持：

```text
USDT入金
USDC入金
```

不支持法币入金。

### 代理端需要实现

- 选择USDT或USDC；
- 选择对应区块链网络；
- 查看平台收款地址；
- 查看或扫描收款二维码；
- 查看币种和网络风险提示；
- 填写实际转账金额；
- 填写链上TxID；
- 提交入金申请；
- 查看申请记录；
- 打开入金详情；
- 查看审核结果和入账时间。

### 管理后台需要实现

- 查看待审核入金；
- 查看代理、币种、网络、金额、TxID和收款地址；
- 核对平台是否实际收到数字货币；
- 审核通过或驳回；
- 填写审核备注或驳回原因；
- 审核通过后增加代理对应币种余额；
- 生成资金流水和处理记录。

### 关键规则

- 暂时不读取区块确认数；
- 暂时不自动监控链上到账；
- 后台人工确认通过后才入账；
- 同一网络下的TxID不能重复入账；
- 入金只审核一次；
- 入金页面不需要展示复杂的处理步骤；
- 入金详情仍需保存申请、审核和入账时间。

## 7. 数字货币兑换模块

> 当前进度：后端代码、订单表、提交冻结、审核完成/解冻和资金流水已完成并通过用户ApiPost测试。用户后续按实际情况所做的代码调整不影响整体流程，当前真实代码为后续开发基线。配置依赖模块5，只读取代理当时的实际比例并保存快照。

第一版只支持：

```text
USDT → USD
USDC → USD
```

不支持USD兑换回USDT或USDC。

### 代理端需要实现

- 选择USDT或USDC；
- 输入兑换数量；
- 显示当前代理适用的兑换比例；
- 自动计算预计获得的USD；
- 显示兑换前后金额；
- 提交兑换申请；
- 查看兑换记录；
- 打开兑换详情；
- 查看审核结果和处理时间。

### 管理后台需要实现

- 复用模块5已经完成的平台默认比例和代理专属比例；
- 查看待审核兑换；
- 查看兑换币种、金额、比例和预计USD；
- 审核通过或驳回；
- 审核通过后完成资产扣减和USD入账；
- 生成对应资金流水；
- 保存处理记录和审核意见。

### 关键规则

- 代理专属比例优先于默认比例；
- 兑换订单必须保存申请时使用的比例；
- 后台修改比例不能影响历史订单；
- 兑换只审核一次；
- 兑换金额不能超过可用余额；
- 兑换提交时立即冻结来源币种，审核通过正式扣除冻结余额，驳回时解冻。

示例：

```text
兑换比例：0.99
兑换数量：1,000 USDT
预计获得：990 USD
```

## 8. 白名单模块

> 当前进度：业务流程、双端原型、后端接口、三张业务表、私有文件存储、状态流转和24小时临时文件清理均已完成，全部接口已于2026-08-13通过用户ApiPost测试。用户已根据实际情况调整部分代码，但不影响整体流程；当前真实代码、migration和接口行为是USD出金及后续业务的开发基线，不重新生成或顺带重构。

白名单分为付款人和收款人，每种再区分企业和个人，共四类：

```text
role 1 + entity_type 1 = 付款人公司
role 1 + entity_type 2 = 付款人个人
role 2 + entity_type 1 = 收款人公司
role 2 + entity_type 2 = 收款人个人
```

固定数字编码：

```text
白名单角色 role：1 付款人，2 收款人
主体类型 entity_type：1 公司，2 个人
企业类型 company_type：1 Non Financial Institute，2 Financial Institute
证件类型 document_type：1 ID，2 Passport
```

国家/地区选项由前端调用第三方API取得，提交选中的文字内容并由数据库原样保存，例如“中国香港”。汇款目的由前端维护数字选项，提交和数据库均只保存对应正整数；第一版不建立汇款目的字典表，也不由后端校验具体编号范围。

代理端和管理后台都必须能够打开白名单详情，查看首次提交资料、每轮文件、双方公开留言和完整处理流程。

### 付款人企业

除证明文件外，以下字段全部必填：

- 公司名称；
- 公司注册国家/地区；
- 公司所在国家/地区；
- 公司所在城市；
- 公司地址；
- 注册时间；
- 企业类型；
- 证件号码。

注册时间由前端提交标准日期文字，例如 `2025-01-07`，数据库按普通字符串保存，后端不校验日期格式。

### 付款人个人

除证明文件外，以下字段全部必填：

- 名称；
- 姓氏；
- 国籍；
- 居住国家/地区；
- 居住城市；
- 居住地址；
- 出生日期；
- 证件类型；
- 证件号码。

出生日期由前端提交标准日期文字，例如 `2025-01-07`，数据库按普通字符串保存，后端不校验日期格式。

### 收款人企业

必填字段：

- 企业名称；
- 所在国家/地区；
- 所在城市；
- 公司地址；
- 银行名称；
- 银行账户号/IBAN；
- SWIFT；
- 汇款目的。

选填字段：

- 中间行SWIFT；
- 备注。

### 收款人个人

必填字段：

- 名称；
- 姓氏；
- 国籍；
- 居住国家/地区；
- 居住城市；
- 居住地址；
- 银行名称；
- 银行账户号/IBAN；
- SWIFT；
- 汇款目的。

选填字段：

- 中间行SWIFT；
- 备注。

除必填、选填和固定数字取值校验外，第一版不对普通业务字段增加长度、格式或日期真实性校验。

### 证明文件与专用上传

四种白名单统一使用一个“上传证明文件”区域，不再要求按商业登记证、护照或银行月结单等固定文件类别分别上传。

文件规则：

```text
支持格式：PDF、PNG、JPG、JPEG
单个文件：最大10MB
首次提交：0至5个，非必传
每轮补交：1至5个，必须至少上传1个
数量限制按每轮独立计算，整个白名单累计文件数允许超过5个
```

新增代理Token专用单文件上传接口，每次请求只接收一个文件，当前请求完成前前端不继续上传下一个文件。文件保存到私有目录，上传成功后立即创建临时文件数据库记录，并向前端返回 `file_id`、原始文件名和文件大小；不向前端暴露私有存储路径。

提交白名单或补交文件时，前端通过 `file_ids` 数组传递本轮已经上传成功的文件。后端必须在同一数据库事务中逐个校验并绑定：

```text
文件属于当前登录代理
文件尚未绑定其他业务或其他轮次
文件上传未超过24小时
本轮file_ids不超过5个且不存在重复ID
每个文件只能绑定一次
```

任意一个文件校验失败时整次提交失败，不能部分绑定。用户在页面移除但最终没有提交的文件继续保持未绑定状态；未绑定超过24小时后允许定时任务同时清理私有实体文件和数据库记录。不提供主动删除临时文件的接口。

未绑定文件没有预览或下载入口。已绑定文件只能通过 `file_id` 和带登录认证的预览/下载接口访问：所属代理和后台管理员可以查看，其他代理无权访问。PDF和图片均支持鉴权预览或下载，响应保留原始文件名；已绑定文件不能删除、替换或覆盖。

### 审核状态与操作

状态编码：

```text
0 待审核
1 待补充文件
2 已通过
3 已驳回
```

状态流转：

```text
代理首次提交 -> 0 待审核
0 待审核 -> 管理员审核通过 -> 2 已通过
0 待审核 -> 管理员审核驳回 -> 3 已驳回
0 待审核 -> 管理员要求补充 -> 1 待补充文件
1 待补充文件 -> 代理补交 -> 0 待审核
1 待补充文件 -> 管理员审核驳回 -> 3 已驳回
```

各状态允许的操作：

- 待审核：管理员可以审核通过、审核驳回或要求补充文件；代理只能查看；
- 待补充文件：代理可以上传1至5个新文件并选填回复；管理员可以直接驳回，但不能审核通过或再次要求补充；
- 已通过、已驳回：双方只能查看，属于最终状态；
- 管理员可以多次要求补充，但必须等待代理完成当前一轮补交并回到待审核后，才能发起下一轮；
- 管理员审核通过备注选填，审核驳回原因必填，要求补充文件的留言必填，代理补交回复选填；
- 管理员审核、驳回和代理补交时必须锁定白名单并再次校验状态，出现并发操作时先完成的一方生效，后完成的一方返回状态已变化。

要求补充文件属于审核过程，不是最终审核。最终通过或驳回只能发生一次。

### 全流程记录

首次提交时不提供代理留言，系统自动生成“代理提交白名单”流程记录，并关联首次提交的0至5个文件。此后每次要求补充、代理补交、审核通过和审核驳回都创建一条不可修改、不可删除的流程记录。

流程记录至少保存：

```text
所属白名单
操作类型：首次提交、要求补充、代理补交、审核通过、审核驳回
操作人类型：代理、管理员
操作人ID
操作人名称快照
公开留言
本轮关联文件
操作时间
```

双方公开留言、文件原始名称和完整流程均对代理与后台可见，第一版不设置仅后台可见的内部备注。管理后台显示真实代理和实际管理员；代理端自己的操作显示代理身份，管理员操作统一显示“平台管理员”，不公开真实管理员姓名。

### 资料修改和代理状态

- 白名单一旦提交，业务资料、已有文件和流程记录均不能修改；
- 待补充阶段只能新增文件和选填回复，不能修改姓名、企业、银行账户等首次提交资料；
- 资料填写错误时由管理员驳回，代理修正后重新创建新的白名单申请；
- 不提供白名单删除接口，历史申请永久保留；同一个主体允许重复提交，不做重复拦截；
- 正常代理可以上传文件、提交白名单和补交文件；暂停或停用代理禁止这些写操作，但仍可查看已有记录；
- 管理员仍可继续处理暂停或停用代理已经提交的记录；处于待补充状态时管理员可以直接驳回，代理恢复正常后可以继续补交。

### 数据表职责

后端实现采用三张业务表：

1. 白名单主表：保存编号、所属代理、角色、主体类型、对应业务资料、当前状态、最终审核管理员、最终审核意见及提交/审核时间；
2. 白名单流程记录表：保存每一步操作类型、操作人、名称快照、公开留言和操作时间；
3. 白名单文件表：上传成功即写入，保存所属代理、可空的白名单及流程记录ID、私有磁盘和相对路径、原始文件名、扩展名、MIME、大小、绑定状态及上传/绑定时间。

白名单主表不使用一个JSON字符串保存所有文件。每个文件关联到首次提交或具体某一轮代理补交记录，以便还原完整审核过程。

### 白名单审核与出金引用

管理后台需要实现：

- 查看待审核白名单；
- 根据四种类型显示不同详情；
- 查看和下载每轮证明文件；
- 查看双方留言及完整处理流程；
- 审核通过、驳回或要求补充文件；
- 保留实际审核管理员、审核意见和审核时间。

已实现接口：

```text
代理端（Agent Token）
POST /web/uploadWhitelistFile
POST /web/submitWhitelist
GET  /web/getWhitelistList
GET  /web/getWhitelistInfo
POST /web/supplementWhitelist
GET  /web/previewWhitelistFile
GET  /web/downloadWhitelistFile

管理后台（Admin Token）
GET  /admin/getWhitelistList
GET  /admin/getWhitelistInfo
POST /admin/requestWhitelistSupplement
POST /admin/reviewWhitelist
GET  /admin/previewWhitelistFile
GET  /admin/downloadWhitelistFile

定时清理命令
php artisan whitelist:cleanup-temp-files
```

只有状态为已通过的白名单可以用于USD出金。出金提交时必须重新校验付款人和收款人均属于当前登录代理并已通过，不能信任前端传入的代理ID。出金订单必须保存提交时付款人和收款人的完整资料快照，后续重新提交同一主体或创建其他白名单不能影响历史订单。

## 9. USD出金模块

> 当前进度：双端原型、数据库迁移、私有文件、完整状态流转、资金流水和全部后端接口已经完成，并已由用户使用ApiPost测试通过。用户之后根据实际情况调整了部分代码，但未改变整体流程；后续以当前真实代码、migration和接口行为为准，不重新生成或顺带重构。

> 配置依赖：模块5已经完成USD固定出金手续费。出金模块只读取当前手续费，并在订单中保存提交时的手续费快照，不重新实现手续费配置接口。

第一版只支持USD出金。

### 代理端需要实现

- 输入出金金额；
- 自动显示固定手续费；
- 自动计算总扣款；
- 选择已审核通过的付款人；
- 选择已审核通过的收款人；
- 通过统一的“上传证明文件”入口选填PDF或图片；
- 提交出金申请；
- 查看出金记录；
- 打开出金详情；
- 查看审核和付款处理结果。

### 管理后台需要实现

- 复用模块5已经完成的USD固定出金手续费；
- 查看待审核出金；
- 查看出金金额、手续费和总扣款；
- 查看付款人和收款人完整资料；
- 查看代理证明文件和管理员付款凭证；
- 审核通过或驳回；
- 填写审核意见或驳回原因；
- 记录后续付款处理结果；
- 生成资金流水和处理时间线。

### 手续费规则

手续费为后台可配置的固定USD金额。

示例：

```text
出金金额：5 USD
固定手续费：50 USD
总扣款：55 USD
```

无论出金金额多少，都需要加上当时配置的固定手续费。

出金订单必须保存申请时的手续费，后续修改配置不能影响历史订单。

### 证明文件和付款凭证规则

- 不再区分同名公司、同一个人或其他付款人/收款人组合；
- 代理端统一提供“上传证明文件”入口，首次提交选填0至5个文件；
- 管理员可要求补件，代理每轮补交1至5个文件并回到待审核，可重复多轮；
- 管理员点击付款完成时不填写付款参考编号，可选上传0至5个付款凭证；
- 付款完成后仍可按轮次追加1至5个付款凭证，代理端可查看；
- 文件只允许PDF、PNG、JPG、JPEG，单个最大10MB，未绑定文件24小时后清理。

### 状态和资金规则

```text
0 待审核
1 待补充文件
2 付款处理中
3 已完成
4 已驳回
5 付款失败
```

- 提交时冻结出金金额和手续费总额；
- 审核通过进入付款处理中，资金继续冻结；
- 审核驳回或付款失败时解冻；
- 付款完成时正式扣除冻结资金；
- 付款失败是终态，原订单不能重试，代理必须重新提交新订单。

出金订单需要保存付款人和收款人在申请时的资料，防止白名单之后发生修改而影响历史订单。

### 已实现接口

```text
GET  /web/getWithdrawalConfig
POST /web/uploadWithdrawalFile
POST /web/submitWithdrawal
GET  /web/getWithdrawalList
GET  /web/getWithdrawalInfo
POST /web/supplementWithdrawal
GET  /web/previewWithdrawalFile
GET  /web/downloadWithdrawalFile

GET  /admin/getWithdrawalList
GET  /admin/getWithdrawalInfo
POST /admin/requestWithdrawalSupplement
POST /admin/reviewWithdrawal
POST /admin/uploadWithdrawalFile
POST /admin/processWithdrawalPayment
POST /admin/appendWithdrawalPaymentFiles
GET  /admin/previewWithdrawalFile
GET  /admin/downloadWithdrawalFile

php artisan withdrawal:cleanup-temp-files
```

## 10. 资产余额模块

> 当前进度：后端代码和migration已经完成，全部接口已通过用户ApiPost测试。用户后续按实际情况所做的代码调整不影响整体流程，当前真实代码是后续资金模块的开发基线。资金冻结、解冻和正式扣款时点已经按第0章口径确认。

需要实现：

- 为代理记录USDT、USDC和USD余额；
- 查询可用余额；
- 如采用冻结机制，记录冻结余额；
- 防止余额不足时继续提交业务；
- 入金审核通过后增加数字货币余额；
- 兑换通过后扣减数字货币并增加USD；
- 出金通过后扣减USD；
- 保证余额更新和资金流水在同一个数据库事务中完成；
- 防止重复入账和重复扣款。

所有资金金额必须使用高精度小数，不能使用浮点数计算。

## 11. 内部余额变动审计（原资金流水模块）

> 当前进度：`fund_flows` 表、Model及事务内写入能力继续作为内部记账审计基线；四个对外只读接口、双端Controller、Facade、筛选查询和总览返回已经删除。前端和管理员页面均不再提供资金流水查询能力。

内部余额审计不等于交易记录：一条审计数据表示一个币种账户中可用余额或冻结余额的一次真实变化；同一笔兑换或出金订单可能产生多条审计记录，尚未产生余额变化的业务订单也可能没有对应审计记录。该数据没有前端接口，统一交易展示全部使用模块12。

需要记录每次余额变化，包括：

- 关联代理；
- 关联业务；
- 业务类型；
- 币种；
- 增加或减少方向；
- 变动金额；
- 变动前余额；
- 变动后余额；
- 发生时间；
- 业务编号；
- 备注。

内部余额变动审计需要满足：

- 不能由普通后台直接修改；
- 不能随业务记录一起删除；
- 错误资金通过新增冲正流水处理；
- 每笔流水可以追溯到具体业务申请。

## 12. 交易记录与详情模块

> 当前进度：双端统一交易列表和统一详情接口已经完成。2026-08-18已新增 `transaction_records` 统一投影表，列表、筛选、分页和近期交易已由原三表 `UNION ALL` 切换为直接查询投影表；三类订单在提交及状态变化时于原业务事务内创建或同步投影。代理资产总览 `recent_orders` 继续复用统一交易格式，管理端可以查询全平台近期交易。双端总览只展示近期交易，对外资金流水接口已经移除。代理端业务总览的近期交易使用整行宽度自适应展示，不需要横向移动。双端CSV导出在第一版原型中隐藏。四个接口的参数和返回结构保持不变，用户已确认本次投影表完善完成。

### 与内部余额变动审计的业务边界

交易记录与内部余额变动审计是两个独立概念：

| 对比项 | 交易记录 | 内部余额变动审计 |
| --- | --- | --- |
| 记录对象 | 一笔入金、兑换或USD出金业务订单 | 一个币种账户的一次真实余额变化 |
| 记录数量 | 一笔业务订单固定展示为一条交易记录 | 一笔业务可能产生一条或多条资金流水 |
| 生成时点 | 业务订单提交成功后即可查询 | 只有发生冻结、解冻、入账或扣款等余额变化时才生成 |
| 是否包含未完成业务 | 包含待审核、待补件、处理中等状态 | 未发生余额变化时不产生对应流水 |
| 是否包含失败业务 | 包含已驳回、付款失败等终态订单 | 只记录该失败过程实际产生的余额变化 |
| 主要用途 | 查看业务订单、状态和详情 | 对账及追溯余额变化 |

统一交易记录第一版只包含：

- 数字货币入金；
- 数字货币兑换USD；
- USD出金。

以下内容不属于统一交易记录：

- 白名单申请：属于业务资料审核，不是资金交易；
- 内部余额变动审计：保留数据但不提供前端接口，不并入交易记录；
- 管理员操作日志：归模块18处理。

### 已实现能力

- 入金、兑换和USD出金已经分别拥有双端列表及详情接口；
- `GET /web/getAgentAssetOverview` 的 `recent_orders` 已返回当前代理最近5笔三类订单；
- `GET /admin/getAgentAssetOverview` 需要指定 `user_id`，只返回单个代理的资产概况和近期订单；管理端运营总览使用独立聚合接口；
- 已实现三类订单统一筛选、统一稳定排序和投影表结果准确分页；
- 已实现统一交易详情入口，同时保留原有三类业务详情接口；
- 当前代理端原型已有“交易记录”和“近期交易”，业务总览的近期交易以整行宽度完整展示；
- 双端原型均保留独立“交易记录”入口；双端总览只展示“近期交易”，点击“查看全部”进入交易记录页。对外资金流水接口已经移除。

### 当前投影表实现方式

- `transaction_records` 是统一只读投影，不是新的业务订单表，也不是内部余额变动审计；
- 原业务订单继续作为业务事实来源，统一详情仍读取原入金、兑换或出金订单；
- 一笔业务订单提交成功时创建一条投影，唯一约束 `business_type + business_id` 和 `order_no` 防止重复记录；
- 投影创建、状态同步、原业务订单、余额变化和内部余额审计必须处于同一个数据库事务，任一环节失败时一起回滚；
- 统一列表直接查询投影表，按 `submitted_at DESC, id DESC` 稳定排序并对完整结果准确分页；
- 投影保存列表需要的币种、网络、金额、兑换比例、手续费、付款人和收款人名称快照，不复制完整业务详情；
- 一笔订单只产生一条交易记录，不因审核、冻结、解冻、完成或失败而增加新的交易记录；
- 状态编码、统一状态组和终态时间由业务Service在原订单状态变化时同步，不能通过Controller或交易模块独立修改；
- 金额、兑换比例、手续费、付款人和收款人名称均来源于原订单提交时快照，不读取后来修改的配置或白名单资料；
- 代理端必须使用Token身份限制为当前代理，不能接受前端传入的代理ID；
- 管理端可以查询全部代理，也可以按指定代理筛选；
- 统一交易模块只读，不提供新增、修改、删除或审核接口。

### 投影创建与状态同步矩阵

| 原业务 | 创建投影 | 后续同步时点 |
| --- | --- | --- |
| 入金 | 提交入金订单后创建 `pending` 投影 | 审核通过同步为 `completed`，驳回同步为 `rejected` |
| 兑换 | 创建兑换订单后、冻结资金的同一事务内创建 `pending` 投影 | 审核通过同步为 `completed`，驳回并解冻时同步为 `rejected` |
| USD出金 | 创建出金订单后、冻结资金的同一事务内创建 `pending` 投影 | 要求补件、代理补交、审核通过/驳回、付款完成/失败时同步对应状态 |

- 已完成出金追加付款凭证不改变交易状态，因此不更新投影状态；
- 所有投影写入均由业务Service显式调用，不使用数据库触发器或隐式Observer；
- 投影写入失败时原订单、余额变动、内部余额审计和操作日志一起回滚；
- 当前三类业务历史数据在实施前已经清空，因此本次不开发历史回填或重建命令。

### 主要实现位置

```text
database/migrations/2026_08_18_100000_create_transaction_records_table.php
app/Models/TransactionRecords.php
app/Services/Agent/TransactionRecordService.php
app/Services/Agent/TransactionService.php
app/Services/Agent/DepositService.php
app/Services/Agent/ExchangeService.php
app/Services/Agent/WithdrawalService.php
```

### 统一列表实际字段

- `transaction_key`：由业务类型和业务ID组成的跨表稳定标识；
- `business_type`、`business_name`：入金、兑换或出金；
- `business_id`、`order_no`：原业务主键和业务编号；
- `user`：管理端返回代理ID、代理编号和企业名称，代理端可沿用同一结构或省略；
- `currency_code`、`network_code`：主币种及网络，非入金业务的网络为空；
- `amount`：入金金额、兑换支付金额或出金收款金额，始终表示订单原始业务金额，不因订单驳回或失败改写为0；
- `target_currency_code`、`target_amount`：仅兑换订单返回实际目标币种和目标金额；
- `exchange_rate`：仅兑换订单返回提交时使用的兑换比例快照；
- `fee_amount`、`total_amount`：仅USD出金返回手续费和账户总冻结/总扣款金额；
- `payer_name`、`payee_name`：仅USD出金返回付款人和收款人名称快照；
- `status`、`status_name`：原业务状态编码和名称；
- `status_group`：跨业务统一筛选状态，建议固定为 `pending`、`needs_supplement`、`processing`、`completed`、`rejected`、`failed`；
- `submitted_at`：订单提交时间；
- `completed_at`：只有成功完成时返回，其他状态为空；
- `finished_at`：成功、驳回或付款失败进入终态的时间，非终态为空；
- `detail_type`、`detail_id`：用于打开对应业务详情。

金额展示建议保持业务含义，不根据是否已经发生资金流水修改订单金额：

```text
入金：显示申请入金金额和币种
兑换：显示支付金额/币种 → 获得金额/USD
出金：显示收款金额、固定手续费和总扣款金额
```

这里的结构化金额不是让前端重新计算金额，而是后端直接返回每种业务已经确定的各项原始金额：

```text
入金：amount = 1000.000000，currency_code = USDT
兑换：amount = 1000.000000，currency_code = USDT，target_amount = 998.00，target_currency_code = USD
出金：amount = 1000.00，fee_amount = 5.00，total_amount = 1005.00，currency_code = USD
```

前端只负责按页面需要组合显示这些金额，不重新计算兑换结果、手续费或总扣款。后端金额字段始终返回无符号十进制字符串；前端也不把待处理、已驳回或付款失败订单显示成已经实际入账或扣款。第一版页面不展示余额实际增减明细；如果后续重新开放此类能力，应另行设计受控的内部审计查询接口，不能根据交易状态推算。

### 统一列表已实现筛选条件

- `business_type`：入金、兑换、出金；
- `user_id`：仅管理端使用；
- `status_group`：使用统一状态组，避免三类订单的原始数字状态含义冲突；
- `currency_code`：按相关交易币种筛选；
- `order_no`：精确查询业务编号；
- `keyword`：管理端可匹配业务编号、代理编号、企业名称或Email；
- `started_at`、`ended_at`：按提交时间筛选；
- `limit`：分页数量，默认15，最大100；
- Laravel分页参数 `page`：由分页器正常处理。

### 已实现接口

```text
GET /web/getTransactionList
GET /web/getTransactionInfo

GET /admin/getTransactionList
GET /admin/getTransactionInfo
GET /admin/getOperationOverview
```

列表接口返回三类订单的统一分页结果。“统一详情接口”是指前端从交易记录列表点击详情时，只调用一个固定入口，并提交 `business_type + business_id`。Service根据 `business_type` 读取现有入金、兑换或出金订单详情，不新建详情表、不复制业务数据，也不改变已有三类详情接口。交易记录详情只允许查看，不提供审核、补件、付款或追加凭证操作；需要操作时必须进入具体业务模块。

示例：

```text
入金详情：business_type=deposit，business_id=12
兑换详情：business_type=exchange，business_id=35
出金详情：business_type=withdrawal，business_id=8
```

统一详情接口已经按确认口径保留，交易记录页面不需要由前端选择三套不同的详情接口；已有的 `getDepositInfo`、`getExchangeInfo`、`getWithdrawalInfo` 继续供各业务自己的列表页使用。统一详情返回：

```text
data.transaction：统一交易公共字段
data.detail：对应入金、兑换或出金的原有完整详情结构
```

当前出金详情中已经注释的 `payer.snapshot`、`payee.snapshot`、`fund_times` 和 `available_actions` 暂不作为接口返回字段。具体业务页面由前端根据订单状态和双方确认的交互规则显示操作；交易记录详情无论状态如何始终只读。

### 双端总览的近期交易

- 代理端继续通过 `GET /web/getAgentAssetOverview` 获取最近5笔交易；为避免破坏已经通过ApiPost测试的返回结构，现有 `recent_orders` 字段默认保留；
- `recent_orders` 已与统一交易列表复用同一套格式化和排序规则，避免首页与交易记录页口径不一致；
- 管理端运营总览调用 `GET /admin/getOperationOverview`，由该接口直接返回全平台最近5笔交易；完整交易记录页面继续调用 `GET /admin/getTransactionList`；
- 双端总览只展示上述近期交易；不返回 `recent_fund_flows`，也不存在对外资金流水查询接口；
- `GET /admin/getAgentAssetOverview?user_id=...` 仍用于查看某个代理的资产概况和近期交易，不等同于全平台运营总览；
- 近7日交易趋势由管理端运营总览接口直接基于 `transaction_records` 按提交日期和业务类型聚合，前端不拉取全部分页数据自行统计。

### 第一版不开发CSV导出

已经确认第一版暂不实现统一交易CSV导出接口。代理端交易记录页和管理端相关页面先隐藏CSV导出按钮，后端本轮也不新增导出接口。以后如重新提出导出需求，再确定导出字段、数据范围和大数据量处理方式。

### 已确认口径

1. 统一交易记录只包含入金、兑换和USD出金，不包含白名单及资金流水；
2. 采用结构化金额字段和无符号金额口径，前端不重新计算金额；
3. 保留统一 `getTransactionInfo` 接口，已有三类业务详情接口保持不变；
4. 双端均保留独立“交易记录”入口，不提供“资金流水”入口或查询接口；
5. 双端总览只展示“近期交易”；代理端近期交易必须完整展示且不需要横向移动；
6. 第一版不开发CSV导出，双端原型隐藏导出按钮。

### 当前验证情况

- 新增代码已通过PHP语法检查，原型共享JavaScript已通过语法检查；
- 四个新路由均位于各自Token认证分组；
- `2026_08_18_100000_create_transaction_records_table` migration已经在当前数据库执行；
- 当前数据库业务订单、内部余额审计和统一交易投影均为空，不需要历史数据回填；
- 已通过事务回滚式数据库验证三类投影创建、状态同步、全量排序、类型筛选、统一状态筛选、币种筛选、管理端关键字筛选和分页金额格式；
- 统一详情继续通过 `business_type + business_id` 调用对应原业务详情Service，未复制业务详情或操作逻辑；
- 投影查询继续以代理Token所属 `user_id` 强制隔离，管理端保留全平台及指定代理筛选；
- 用户已于2026-08-14使用ApiPost完成原聚合实现下的四个统一交易接口及资产总览近期交易验证；2026-08-18确认 `transaction_records` 投影表完善完成，四个接口契约保持不变。
- 用户已于2026-08-18使用ApiPost完成 `GET /admin/getOperationOverview` 验证；正常代理、三币种账面余额、四类待处理业务、近7日趋势和最近5笔交易的当前返回结构已经确认。

## 13. 审核中心模块

> 当前进度：入金、兑换、白名单和USD出金的后台列表、详情、审核及并发防重已经随对应业务完成。当前按业务入口分别处理，统一审核中心聚合页或聚合接口尚未单独开发，也不是下一默认模块。

管理后台需要统一处理：

- 入金审核；
- 兑换审核；
- 白名单审核；
- 出金审核。

需要实现：

- 待审核数量；
- 按业务类型筛选；
- 打开完整详情；
- 审核通过；
- 审核驳回；
- 填写审核意见；
- 记录审核管理员；
- 记录审核时间；
- 防止重复审核；
- 审核操作与资金处理使用数据库事务。

入金和兑换只审核一次；白名单与USD出金允许按已确认流程多轮补件，但最终通过或驳回只处理一次。

## 14. 处理时间线模块

> 当前进度：白名单和USD出金已经保存独立、不可修改的完整流程记录；入金和兑换通过订单状态、处理时间和资金流水追溯。第一版不额外创建所有业务共用的时间线表。

所有主要业务需要保存处理事件，例如：

```text
用户提交申请
系统校验成功
等待后台审核
管理员审核通过
管理员审核驳回
资金入账
资金扣减
付款处理中
付款完成
付款失败
```

时间线需要记录：

- 业务类型；
- 业务ID；
- 事件类型；
- 事件标题；
- 事件说明；
- 操作人；
- 发生时间。

代理端只展示适合代理查看的信息，后台可以查看更完整的内部处理记录。

## 15. 文件与附件模块

> 当前进度：白名单证明文件、USD出金证明文件和管理员付款凭证的私有上传、单次绑定、预览、下载、归属校验及24小时未绑定文件清理已经完成，并随对应业务通过ApiPost测试。第一版按业务独立存储，不建立全局附件中心。

需要统一管理：

- 白名单企业文件；
- 白名单个人文件；
- 护照；
- 银行月结单；
- 商业登记证；
- 公司注册证明书；
- 周年申报表；
- 董事护照；
- 出金证明文件；
- 出金付款凭证。

需要实现：

- PDF、PNG、JPG格式校验；
- 文件大小限制；
- 文件安全命名；
- 文件上传；
- 文件预览或下载；
- 登录和访问权限检查；
- 记录上传人和上传时间；
- 防止用户访问其他代理的文件；
- 业务记录保留时，相关附件不能被随意删除。

## 16. 邮件与通知模块

> 当前进度：代理账户邀请、重新发送邀请、密码重置和登录Email变更通知已完成；入金、兑换、白名单和USD出金业务结果通知尚未开发。用户已确认本模块暂缓，不作为第一版后端接口收尾模块；如后续重新启动，开发前仍需重新确认通知矩阵。

根据最终账号方案，需要支持部分或全部通知：

- 代理账号邀请；
- 重新发送邀请；
- 密码重置；
- 登录Email变更通知；
- 账号停用通知；
- 白名单审核结果；
- 入金审核结果；
- 兑换审核结果；
- 出金审核结果。

需要记录：

- 收件人；
- 邮件类型；
- 发送状态；
- 发送次数；
- 发送时间；
- 失败原因。

开发前必须与用户确认每种业务的触发节点、收件人、通过/驳回/补件/付款结果模板、失败重试策略和第一版必发范围，不自行补全通知矩阵。

## 17. 系统字典模块

> 当前口径：第一版不开发独立的系统字典后端模块。

企业类型、汇款目的、证件类型、国家/地区等具体内容由用户与前端人员对齐。除非用户后续明确重新提出需求，不创建字典表、Model、Service、Controller或维护接口。

历史白名单和业务订单仍需保存提交时实际使用的编码及显示值快照，不能依赖前端选项之后保持不变。

## 18. 操作日志与安全模块

> 当前进度：现有代理登录安全日志和临时锁定保持不变。用户已将模块18第一版范围收窄为简单的管理员操作记录，不新增管理员登录安全、资金审计聚合或复杂日志详情。管理端新版原型只保留管理员操作记录；资金流水对外接口已经移除，内部余额变动审计数据继续保留。管理员操作日志表、Model、Facade、Service、Controller、Token保护的列表路由以及重要管理操作的事务内写入已经完成，并已通过PHP语法、路由、migration预执行和静态检查。用户已确认第一阶段完成，本模块作为第一版后端接口收尾能力纳入当前基线。

### 已实现能力

- 新增 `admin_operation_logs` 管理员操作日志表，保存管理员ID、姓名和Email快照、模块、操作类型、操作内容、操作对象、IP和操作时间；
- 新增 `AdminOperationLogs` Model，历史日志不提供修改和删除能力；
- 新增 `OperationLog` Facade/别名、`OperationLogService` 和 `OperationLogController`，保持 `Route -> Controller -> Facade -> Service -> Model -> Database` 分层；
- 新增管理端只读分页接口 `GET /admin/getOperationLogList`，始终使用 `admin.agent.token` 校验后台Token；
- 管理员、代理账户、资产配置、入金审核、兑换审核、白名单审核和USD出金处理的重要成功操作均在原业务事务内写入一条日志；
- 入金、兑换、白名单和USD出金审核已恢复使用Token中的真实管理员ID，不再临时固定为管理员ID `1`；
- 普通查询、文件预览/下载、尚未绑定业务的管理员付款凭证上传、代理端操作及失败回滚请求不写入日志。

### 第一版业务边界

- 操作记录只记录管理端成功执行的重要操作，不记录代理操作；
- 代理提交、补件等行为继续通过原业务订单、白名单/出金时间线和资金流水追溯；
- 普通列表、详情查询、文件预览/下载、尚未绑定业务的临时文件上传不记录操作日志；
- 参数校验失败、状态冲突及事务回滚等未产生数据变化的请求不记录操作日志；
- 一次管理端接口成功调用只生成一条操作日志，即使该操作同时更新多张表、产生多条资金流水或绑定多个文件；
- 操作日志永久保留，不提供修改、删除或CSV导出接口。

需要记录管理员的重要操作：

- 创建、修改、删除、启用或停用管理员；
- 创建或修改代理；
- 启用、暂停或停用账号；
- 重新发送代理邀请或发起密码重置；
- 启用或停用币种网络关系；
- 修改兑换比例；
- 修改出金手续费；
- 修改收款地址；
- 审核通过或驳回入金；
- 审核通过或驳回兑换；
- 要求白名单补件、审核通过或驳回白名单；
- 要求USD出金补件、审核通过或驳回USD出金；
- 将USD出金标记为付款完成或付款失败；
- 为已完成USD出金追加付款凭证。

日志应包括：

- 管理员ID、姓名和Email快照；
- 操作模块；
- 操作类型；
- 操作内容；
- 操作对象类型、ID或业务编号；
- IP；
- 操作时间。

### 写入规则

- 操作日志由实际完成业务处理的Service写入，不能由Controller直接根据请求参数推测结果；
- 账号、配置、审核、付款处理和对应操作日志必须在同一个数据库事务中完成；日志写入失败时业务变更一起回滚；
- 操作内容由后端根据实际结果生成简短中文描述，例如“审核通过入金 DEP-xxx”或“修改USD固定出金手续费”；
- 密码、密码哈希、Token、私有文件路径及文件实体内容不得写入操作日志；
- 资金业务只记录管理员执行了对应审核或付款操作，不关联展示资金流水，也不新增第二套资金审计数据；
- 审核Service已经恢复使用Token中的真实管理员ID；后续不得重新固定管理员ID或信任前端传入的管理员ID。

### 第一版不包含

- 管理员登录成功或失败记录；
- 管理员登录失败次数限制和临时锁定；
- 统一查询管理员与代理登录安全日志；
- 操作日志修改前后数据对比；
- 操作日志详情接口；
- 资金审计聚合或资金流水关联展示；
- CSV导出。

### 第一版查询接口

```text
GET /admin/getOperationLogList
```

- 列表按操作时间倒序分页返回，第一版只需要分页参数 `limit`；
- 接口必须放入始终启用的管理后台Token认证分组；
- 代理端不提供操作日志查询接口。

## 19. 页面状态与基础体验

代理端和管理后台都需要满足：

- 刷新后保持登录；
- 刷新后尽量停留在当前页面；
- 刷新只重新获取最新数据；
- 表单提交时防止重复点击；
- 后台审核时防止重复处理；
- 列表有加载、空数据和错误状态；
- 金额和状态显示一致；
- 代理端以繁体中文为主要界面语言；
- 关键业务有明确成功、失败和等待审核提示。

## 20. 第一版明确不包含

第一版暂不包含：

- 法币入金；
- 代理内部转账；
- 代理下级客户；
- 代理发展下线；
- 企业KYB自动认证；
- 个人KYC自动认证；
- 代理团队成员；
- 复杂管理员角色权限；
- 自动读取区块确认数；
- 自动链上入账；
- 自动执行银行付款；
- 第三方KYC接口；
- 第三方银行接口。

## 21. 给新开发对话的说明

本文件同时记录业务范围和当前开发进度。第0章是当前进度基线，但最终仍以实际代码、migration和测试结果为准。

新开发对话需要：

1. 先阅读 `AGENTS.md`、第0章和实际代码；
2. 默认模块2代理认证、模块3代理账户管理、模块4代理资产总览、模块5业务配置、模块6数字货币入金、模块7数字货币兑换、模块8白名单、模块9 USD出金、模块10资产余额和模块12交易记录与详情已经完成并通过用户ApiPost测试，不重新生成或重构；原模块11仅保留内部余额变动审计写入，不再提供对外查询接口；
3. 模块12“交易记录与详情”已经完成双端统一列表、统一详情、总览近期交易复用和双端原型；`transaction_records` 投影表、三类业务事务内创建/状态同步及统一查询切换已经完成；管理端运营总览聚合接口也已补齐并通过用户ApiPost测试；
4. 模块13至15的审核、时间线和私有文件能力已经按业务分别落地；除非用户明确要求，不额外创建统一时间线表、全局附件中心或统一审核中心聚合接口；
5. 模块18“操作日志与安全”简化版代码已经完成，第一阶段后端接口开发已告一段落；模块16邮件与通知、管理员登录安全扩展和统一审核中心等其他能力继续暂缓，除非用户明确重新启动；
6. 不使用本文件推断数据库表结构或认证存储方式；
7. 不覆盖或回退用户已有修改；
8. 模块5固定矩阵、收款地址、兑换比例和手续费，以及模块6、7、9、11的订单和资金处理方式均以当前真实代码为准；后续业务必须保留配置及必要业务资料快照；
9. 每完成一个模块都执行PHP语法检查、路由核对和必要的静态检查，并提供ApiPost测试说明供用户验证；除非用户明确要求，不在`tests/`目录创建接口测试用例；
10. 后续业务必须复用现有代理身份和Token隔离逻辑，不创建第二套代理账号体系；
11. 当前出金资金口径是提交时冻结、审核驳回或付款失败时解冻、实际付款完成时正式扣除；付款失败原订单不能重试，不得自行改变该口径；
12. 第一版不开发系统字典后端，除非用户重新明确提出需求。
