# MTPay 前端 AI 接口文档

> 文档用途：交给前端同事及其 AI，作为当前第一版管理端和代理端的接口接入说明。
>
> 生成依据：`routes/`、Controller 参数校验、Service 返回组装、Model 状态常量和中间件真实代码。
>
> 核对日期：2026-08-18。接口路径均为当前代码中的真实路径，不根据原型模拟数据推测。

## 1. 前端 AI 必须先遵守的规则

1. 基础地址统一写成 `{BASE_URL}`，由前端环境变量配置。例如：`https://example.com`。
2. 管理端业务接口前缀是 `/admin`，代理端业务接口前缀是 `/web`。不要擅自在它们前面再加 `/api`。
3. 只有通用工具接口使用 `/api` 前缀，例如 `/api/getPubKey`。
4. 管理端 Token 和代理端 Token 不是同一种身份，不能混用。
5. 登录后请求统一携带请求头：`Authorization: Bearer <token>`。
6. 若任意 Token 接口的响应头中出现新的 `Authorization`，必须用它覆盖本地旧 Token。服务端可能在 Token 刷新期自动续签。
7. JSON 接口不能只根据 HTTP 状态判断成功。以响应体的 `status` 为准，并兼容数字和数字字符串：`Number(status) === 200`。
8. 所有金额、余额、手续费和兑换比例都按十进制字符串处理，例如 `"100.00"`。不要转成 JavaScript `Number` 后进行资金计算。
9. GET 接口参数放 query；普通 POST 接口使用 JSON；文件上传使用 `multipart/form-data`。
10. 列表接口默认每页 15 条。`page` 为页码，`limit` 为每页数量，`limit` 通常为 1～100。
11. 日期筛选格式为 `Y-m-d`，例如 `2026-08-14`；返回时间格式为 `Y-m-d H:i`，无时间时返回 `null`。
12. `started_at` 与 `ended_at` 同时存在时，开始日期不能晚于结束日期；结束日期包含当天 23:59:59。
13. 交易记录包含入金、兑换、出金三类订单。独立“交易记录”页面使用 `/getTransactionList`、`/getTransactionInfo`；代理总览从 `/web/getAgentAssetOverview` 的 `recent_orders` 读取近期交易；管理端运营总览一次调用 `/admin/getOperationOverview`；具体业务页面继续使用对应业务接口。
14. 余额变动审计仅供后端内部记账和查错，不再提供管理端或代理端查询接口，也不属于前端接入范围。
15. 交易记录详情始终是只读入口，只展示对应业务详情，不显示审核、补件、付款、追加凭证等业务操作；需要操作时进入具体业务模块。

## 2. 统一请求与响应

### 2.1 请求头

无需登录的 JSON 接口：

```http
Accept: application/json
Content-Type: application/json
```

需要登录的 JSON 接口：

```http
Accept: application/json
Content-Type: application/json
Authorization: Bearer <token>
```

需要登录的上传接口：

```http
Accept: application/json
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

浏览器生成 multipart 请求时不要手工拼接 boundary，让浏览器或请求库自动生成 `Content-Type`。

### 2.2 成功响应

绝大多数业务接口返回：

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {},
  "is_popup": false,
  "sign": ""
}
```

- `data` 可能是对象、数组、分页对象、字符串或空数组。
- `is_popup: true` 表示前端可以按产品交互显示成功提示；`false` 通常为静默读取。
- 个别旧通用接口没有 `is_popup` 和 `sign`，以各接口说明为准。

### 2.3 失败与参数校验响应

```json
{
  "status": 422,
  "message": "第一个参数错误说明",
  "is_popup": true,
  "data": []
}
```

业务错误结构相同，只是 `status` 为对应业务错误码。前端统一显示 `message`，不能只判断 HTTP 状态。

### 2.4 分页响应

所有列表接口的 `data` 使用 Laravel 分页结构：

```json
{
  "current_page": 1,
  "data": [],
  "first_page_url": "...",
  "from": 1,
  "last_page": 1,
  "last_page_url": "...",
  "next_page_url": null,
  "path": "...",
  "per_page": 15,
  "prev_page_url": null,
  "to": 1,
  "total": 1
}
```

前端列表数据读取 `response.data.data.data`，总数读取 `response.data.data.total`。

### 2.5 文件响应

预览和下载接口成功时返回文件二进制，不返回统一 JSON：

- 预览：`Content-Disposition: inline`
- 下载：`Content-Disposition: attachment`
- 请求失败时仍返回统一 JSON 错误结构。
- 需要鉴权的文件不能直接把 URL 赋给 `<img>` 或 `<a>` 后期望自动带 Token；应使用带 `Authorization` 的请求获取 Blob，再创建临时 URL。

## 3. 登录加密协议

管理端 `/admin/adminLogin` 和代理端 `/web/agentLogin` 不能直接提交邮箱和密码，必须先获取一次性公钥并生成加密信封。

### 3.1 获取一次性公钥

`GET {BASE_URL}/api/getPubKey`

无参数、无 Token。返回：

```json
{
  "status": 200,
  "message": "get success",
  "data": {
    "public": "MIGfMA0GCSqGSIb3DQEBAQUAA4GN..."
  }
}
```

`data.public` 是去掉 PEM 头尾和换行后的 RSA 公钥正文。该公钥约 1 分钟有效，并且私钥在第一次解密时被取出，因此一个公钥只用于一次登录请求。

### 3.2 构造登录信封

前端按以下顺序处理：

1. 生成 32 位随机 ASCII 字符串，记为 `aesKey`。
2. 将 `{"email":"...","password":"..."}` 序列化为 JSON。
3. 使用 `AES-256-CBC` 加密 JSON；Key 为 `aesKey`，IV 固定为 `1234567887654321`，使用 PKCS#7 padding；密文进行 Base64，得到 `data`。
4. 将公钥正文补成标准 PEM 公钥，使用 RSA PKCS#1 v1.5 加密 `aesKey`，密文进行 Base64，得到 `random`。
5. 对服务端原样返回的公钥正文计算 MD5，得到 `pubKeyMd5`。
6. 将下列 JSON 提交登录接口：

```json
{
  "random": "RSA 加密后的 AES Key Base64",
  "pubKeyMd5": "公钥正文的 MD5",
  "data": "AES 加密后的登录 JSON Base64"
}
```

登录加密失败或公钥过期时，重新调用 `/api/getPubKey`，不要复用旧公钥。

## 4. 公共枚举和状态流转

### 4.1 代理状态

| 值 | 含义 | 可流转到 |
|---:|---|---|
| 0 | 待激活 | 3 停用 |
| 1 | 正常 | 2 暂停、3 停用 |
| 2 | 暂停 | 1 正常、3 停用 |
| 3 | 停用 | 终态 |

### 4.2 入金状态

| 值 | 含义 |
|---:|---|
| 0 | 待审核 |
| 1 | 已入账 |
| 2 | 已驳回 |

### 4.3 兑换状态

| 值 | 含义 |
|---:|---|
| 0 | 待审核 |
| 1 | 已完成 |
| 2 | 已驳回 |

兑换提交时立即冻结来源币种；审核通过后扣除冻结来源币种并增加 USD；驳回后释放冻结金额。

### 4.4 白名单状态和类型

- `role`：`1` 付款人，`2` 收款人。
- `entity_type`：`1` 公司，`2` 个人。
- `company_type`：`1` 非金融机构，`2` 金融机构。
- `document_type`：`1` 身份证件，`2` 护照。

| 状态值 | 含义 |
|---:|---|
| 0 | 待审核 |
| 1 | 待补充文件 |
| 2 | 已通过 |
| 3 | 已驳回 |

白名单流程：代理提交 → 管理员要求补件或直接审核 → 代理补件后回到待审核 → 管理员通过或驳回。

### 4.5 USD 出金状态

| 值 | 含义 |
|---:|---|
| 0 | 待审核 |
| 1 | 待补充文件 |
| 2 | 付款处理中 |
| 3 | 已完成 |
| 4 | 已驳回 |
| 5 | 付款失败 |

关键规则：

- 提交出金时冻结 `出金金额 + 固定手续费`。
- 初次证明文件非必填，不区分是否同名公司或同一个人。
- 管理员可在待审核状态要求代理补件。
- 审核通过进入付款处理中；审核驳回释放冻结资金。
- 管理员付款完成时可选上传付款凭证；完成后仍可追加付款凭证供代理查看。
- 付款失败会释放冻结资金，且不能在原订单重试。需要重新发起一笔出金。

### 4.6 统一交易状态组

`status_group` 用于跨业务筛选：

- `pending`：待审核
- `needs_supplement`：待补充文件
- `processing`：处理中
- `completed`：已完成
- `rejected`：已驳回
- `failed`：失败

## 5. 公共返回对象

以下对象会被多个接口复用。每个接口章节会说明 `data` 返回哪一种对象。

### 5.1 `AgentAccount`

```json
{
  "id": 1,
  "agent_code": "AG00000001",
  "company_name": "Example Ltd",
  "email": "agent@example.com",
  "phone": "+85212345678",
  "status": 1,
  "status_name": "正常",
  "activated_at": "2026-08-14 10:00",
  "last_login_at": "2026-08-14 12:00",
  "locked_until": null,
  "created_at": "2026-08-14 09:00"
}
```

代理端 Profile 不返回 `locked_until` 和 `created_at`。管理端新增代理时额外返回 `invitation`；编辑代理时额外返回 `mail_notifications`。

### 5.2 `MailResult`

```json
{
  "id": 1,
  "mail_type": "invitation",
  "to_email": "agent@example.com",
  "attempt_no": 1,
  "status": 1,
  "status_name": "发送成功",
  "failure_reason": null,
  "sent_at": "2026-08-14 10:00",
  "created_at": "2026-08-14 10:00"
}
```

`mail_type` 为 `invitation`、`password_reset` 或 `email_changed`；邮件状态为 `0` 发送中、`1` 成功、`2` 失败。

### 5.3 `AgentAssets`

```json
{
  "user_id": 1,
  "agent_code": "AG00000001",
  "company_name": "Example Ltd",
  "email": "agent@example.com",
  "status": 1,
  "status_name": "正常",
  "assets": [
    {
      "currency_id": 1,
      "currency_code": "USDT",
      "currency_name": "Tether",
      "decimal_places": 6,
      "available_balance": "100.000000",
      "frozen_balance": "0.000000",
      "total_balance": "100.000000"
    }
  ]
}
```

### 5.4 `AssetOverview`

```json
{
  "user": {
    "id": 1,
    "agent_code": "AG00000001",
    "company_name": "Example Ltd",
    "email": "agent@example.com",
    "status": 1,
    "status_name": "正常"
  },
  "assets": [
    {
      "currency": {"id": 1, "code": "USDT", "name": "Tether", "decimal_places": 6},
      "available_balance": "100.000000",
      "frozen_balance": "0.000000",
      "total_balance": "100.000000"
    }
  ],
  "effective_exchange_rates": {
    "USDT": {
      "source_currency": {"id": 1, "code": "USDT", "name": "Tether"},
      "target_currency": {"id": 3, "code": "USD", "name": "US Dollar"},
      "rate": "1.000000000000",
      "rate_source": "default",
      "rate_source_name": "平台默认"
    },
    "USDC": {}
  },
  "pending_counts": {"deposit": 0, "exchange": 0, "withdrawal": 0, "total": 0},
  "capabilities": {
    "deposit_channel_count": 2,
    "exchange_source_currencies": ["USDT", "USDC"],
    "exchange_target_currency": "USD",
    "withdrawal_currency": "USD",
    "withdrawal_fee_amount": "10.00"
  },
  "recent_orders": []
}
```

`recent_orders` 固定最多 5 条，元素为 `TransactionItem`。资产总览不返回余额变动审计记录。

`recent_orders` 与统一交易列表读取同一套 `transaction_records` 投影和格式化逻辑，前端不需要自行合并入金、兑换和出金列表。

### 5.5 `OperationOverview`

```json
{
  "agent_summary": {
    "total_count": 20,
    "active_count": 18,
    "active_percentage": 90
  },
  "balance_totals": [
    {
      "currency": {
        "id": 1,
        "code": "USDT",
        "name": "Tether USD",
        "type": 1,
        "type_name": "数字货币",
        "decimal_places": 6
      },
      "available_balance": "1000.000000",
      "frozen_balance": "200.000000",
      "total_balance": "1200.000000",
      "frozen_percentage": "16.66"
    }
  ],
  "pending_businesses": {
    "deposit": 1,
    "exchange": 2,
    "whitelist": 3,
    "withdrawal": 4,
    "total": 10
  },
  "transaction_trend": {
    "days": 7,
    "started_at": "2026-08-12",
    "ended_at": "2026-08-18",
    "total": 12,
    "items": [
      {
        "date": "2026-08-12",
        "deposit": 1,
        "exchange": 0,
        "withdrawal": 2,
        "total": 3
      }
    ]
  },
  "recent_transactions": []
}
```

- `agent_summary` 统计当前未删除代理；`active_count` 只统计状态为 `1` 的正常代理，百分比返回四舍五入后的整数。
- `balance_totals` 固定按币种顺序汇总当前未删除代理的账面余额，包含各种代理状态；金额和 `frozen_percentage` 都是字符串。
- 待处理入金、兑换为待审核；白名单为待审核或待补充文件；出金为待审核、待补充文件或付款处理中。
- `transaction_trend` 固定返回包含今天在内的最近7个自然日，按订单 `submitted_at` 统计笔数。无交易日期也返回一项且数量为0；图表悬停直接读取当天的 `deposit`、`exchange`、`withdrawal`。
- `recent_transactions` 固定最多5项，元素结构为 `TransactionItem`，按 `submitted_at DESC, id DESC` 排序。

### 5.6 `DepositOrder`

列表和详情公共字段：

```json
{
  "id": 1,
  "order_no": "订单号",
  "user": {"id": 1, "agent_code": "AG00000001", "company_name": "Example Ltd", "email": "agent@example.com"},
  "currency": {"id": 1, "code": "USDT", "name": "Tether", "decimal_places": 6},
  "network": {"id": 1, "code": "TRC20", "name": "TRON"},
  "amount": "100.000000",
  "txid": "区块链交易哈希",
  "receiving_address_snapshot": "提交时平台收款地址",
  "status": 0,
  "status_name": "待审核",
  "submitted_at": "2026-08-14 10:00",
  "updated_at": "2026-08-14 10:00"
}
```

详情和提交/审核成功响应额外返回：

```json
{
  "review": {"admin_id": 1, "admin_name": "Admin", "note": null, "reviewed_at": null},
  "credited_at": null,
  "timeline": [{"event": "submitted", "name": "已提交", "time": "2026-08-14 10:00"}]
}
```

### 5.7 `ExchangeOrder`

```json
{
  "id": 1,
  "order_no": "订单号",
  "user": {"id": 1, "agent_code": "AG00000001", "company_name": "Example Ltd", "email": "agent@example.com"},
  "source_currency": {"id": 1, "code": "USDT", "name": "Tether", "decimal_places": 6},
  "target_currency": {"id": 3, "code": "USD", "name": "US Dollar", "decimal_places": 2},
  "source_amount": "100.000000",
  "exchange_rate": "1.000000000000",
  "rate_source": "default",
  "rate_source_name": "平台默认",
  "target_amount": "100.00",
  "status": 0,
  "status_name": "待审核",
  "submitted_at": "2026-08-14 10:00",
  "frozen_at": "2026-08-14 10:00",
  "updated_at": "2026-08-14 10:00"
}
```

详情和提交/审核成功响应额外返回 `review`、`completed_at`、`timeline`。`rate_source` 为 `agent` 或 `default`。

### 5.8 `TransactionItem`

```json
{
  "transaction_key": "deposit:1",
  "business_type": "deposit",
  "business_name": "入金",
  "id": 1,
  "business_id": 1,
  "order_no": "订单号",
  "user": {"id": 1, "agent_code": "AG00000001", "company_name": "Example Ltd", "email": "agent@example.com"},
  "currency_code": "USDT",
  "network_code": "TRC20",
  "amount": "100.000000",
  "target_currency_code": null,
  "target_amount": null,
  "exchange_rate": null,
  "fee_amount": null,
  "total_amount": null,
  "payer_name": null,
  "payee_name": null,
  "status": 0,
  "status_name": "待审核",
  "status_group": "pending",
  "status_group_name": "待审核",
  "submitted_at": "2026-08-14 10:00",
  "completed_at": null,
  "finished_at": null,
  "detail_type": "deposit",
  "detail_id": 1
}
```

字段按业务类型填充：

- 入金：`currency_code`、`network_code`、`amount` 有值。
- 兑换：来源币种放 `currency_code/amount`，USD 放 `target_currency_code/target_amount`，并返回 `exchange_rate`。
- 出金：USD 放 `currency_code/amount`，并返回 `fee_amount`、`total_amount`、`payer_name`、`payee_name`。

统一交易详情返回：

```json
{
  "transaction": {},
  "detail": {}
}
```

其中 `transaction` 为 `TransactionItem`，`detail` 按 `business_type` 分别是 `DepositOrder`、`ExchangeOrder` 或 `WithdrawalOrder`。

后端已经使用 `transaction_records` 统一投影表保存列表公共字段。一笔业务订单固定对应一条交易记录，审核、补件、完成、驳回或付款失败只更新原记录状态，不产生第二条交易记录。该实现不改变接口字段：

- `transaction_key` 仍由 `business_type:business_id` 组成，不是投影表主键；
- `id`、`business_id` 和 `detail_id` 仍表示原业务订单ID；
- `status_changed_at` 是后端投影同步字段，当前接口不返回，前端不要依赖；
- 金额、比例、手续费和名称均由后端返回，前端不得根据状态重新计算或改写；
- 统一详情接口仅用于查看。即使当前状态在具体业务模块允许操作，交易记录详情也不能显示业务操作按钮。

### 5.9 `WhitelistItem`

列表字段：

```json
{
  "id": 1,
  "whitelist_no": "白名单编号",
  "user": {"id": 1, "agent_code": "AG00000001", "company_name": "Example Ltd", "email": "agent@example.com"},
  "role": 1,
  "role_name": "付款人",
  "entity_type": 1,
  "entity_type_name": "公司",
  "subject_name": "Example Ltd",
  "country": "HK",
  "status": 0,
  "status_name": "待审核",
  "file_count": 1,
  "submitted_at": "2026-08-14 10:00",
  "updated_at": "2026-08-14 10:00"
}
```

详情额外返回：

```json
{
  "business_data": {},
  "review": {"admin_id": 1, "admin_name": "Admin", "note": null, "reviewed_at": null},
  "records": [
    {
      "id": 1,
      "action_type": 1,
      "action_name": "代理提交白名单",
      "actor_type": 1,
      "actor_type_name": "代理",
      "actor_id": 1,
      "actor_name": "Example Ltd",
      "message": null,
      "files": [],
      "created_at": "2026-08-14 10:00"
    }
  ]
}
```

白名单文件对象：

```json
{
  "file_id": 1,
  "original_name": "document.pdf",
  "extension": "pdf",
  "mime_type": "application/pdf",
  "size": 1024,
  "uploaded_at": "2026-08-14 10:00"
}
```

### 5.10 `WithdrawalOrder`

列表字段：

```json
{
  "id": 1,
  "order_no": "订单号",
  "user": {"id": 1, "agent_code": "AG00000001", "company_name": "Example Ltd", "email": "agent@example.com"},
  "currency": {"id": 3, "code": "USD", "name": "US Dollar", "decimal_places": 2},
  "amount": "100.00",
  "fee_amount": "10.00",
  "total_amount": "110.00",
  "payer": {"whitelist_id": 1, "whitelist_no": "...", "entity_type": 1, "name": "Payer Ltd"},
  "payee": {"whitelist_id": 2, "whitelist_no": "...", "entity_type": 1, "name": "Payee Ltd"},
  "status": 0,
  "status_name": "待审核",
  "application_file_count": 0,
  "payment_file_count": 0,
  "submitted_at": "2026-08-14 10:00",
  "updated_at": "2026-08-14 10:00"
}
```

详情额外返回：

```json
{
  "review": {"admin_id": 1, "admin_name": "Admin", "note": null, "reviewed_at": null},
  "payment": {
    "admin_id": null,
    "admin_name": null,
    "failure_reason": null,
    "processing_at": null,
    "completed_at": null,
    "failed_at": null
  },
  "application_files": [],
  "payment_files": [],
  "records": []
}
```

当前出金详情不返回 `payer.snapshot`、`payee.snapshot`、`fund_times` 或 `available_actions`。具体出金业务页面由前端根据第4.5节状态和产品交互决定操作按钮；统一交易详情无论状态如何都只读。

出金文件对象：

```json
{
  "file_id": 1,
  "file_type": 1,
  "file_type_name": "证明文件",
  "uploader_type": 1,
  "uploader_name": "Example Ltd",
  "original_name": "proof.pdf",
  "extension": "pdf",
  "mime_type": "application/pdf",
  "size": 1024,
  "uploaded_at": "2026-08-14 10:00",
  "bound_at": null
}
```

- `file_type`：`1` 代理证明文件，`2` 管理员付款凭证。
- `uploader_type`：`1` 代理，`2` 管理员。
- 代理端响应会隐藏管理员真实 ID，并把管理员名称显示为“平台管理员”。

## 6. 通用工具接口

这些接口没有登录要求。测试接口仅用于联调诊断，不应被生产页面依赖。

| 接口 | 参数 | `data` / 返回内容 |
|---|---|---|
| `GET /api/getPubKey` | 无 | `{public: string}`；登录一次性 RSA 公钥，见第 3 节 |
| `POST /api/testEncryption` | JSON：`public_key` 必填，其他字段任意 | 加密测试信封：`status/message/data/sign/random/pubKeyMd5`；仅调试 |
| `POST /api/testDecrypt` | JSON：`random`、`pubKeyMd5`、`data` | 直接返回解密后的内容；会消耗一次性私钥，仅调试 |
| `POST /api/aesDecrypt` | JSON：`data`、`random`；其中 `random` 是明文 AES Key | 直接返回 AES 解密文本，仅调试 |
| `GET /api/isPopUp` | query：`type` 可选；值为 `1` 时弹窗 | 标准响应，`data=[]`，`is_popup` 根据 `type` 返回 |
| `GET /api/getAreaCode` | 无 | 数组，每项 `{code_id, code_name}` |
| `GET /api/getGoogleLocation` | query：`latitude`、`longitude`、`language` | Google Geocode 原始 JSON 放在 `data`；依赖服务端 Google Key |
| `GET /api/getGoogleSearchAddress` | query：`keyword`、`language` | Google Places Text Search 原始 JSON放在 `data` |
| `POST /api/uploadImage` | multipart：`file`，`folder` 可选 | 成功时 `data` 为 `storage/...` 路径。业务证明文件不要用这个接口 |

## 7. 管理端接口

除 `/admin/adminLogin` 外，管理端前端统一携带管理端 Token。旧管理员 CRUD 的服务端鉴权仍受历史配置开关影响，但客户端不得依赖免 Token 行为。

### 7.1 登录和管理员账户

#### `POST /admin/adminLogin`

- 鉴权：无。
- Content-Type：JSON。
- 参数：登录加密信封 `random`、`pubKeyMd5`、`data`，见第 3 节。信封明文为 `email`、`password`。
- 返回：

```json
{
  "id": 1,
  "email": "admin@example.com",
  "name": "Admin",
  "token": "管理端 Token"
}
```

#### 管理员 CRUD

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `POST /admin/addAdmin` | `name` 必填；`email` 必填且合法；`password` 必填，8～20 位且只含大小写字母和数字，并同时包含大写、小写、数字 | `[]` |
| `GET /admin/getAdminList` | query：`keyword` 可选（姓名/邮箱模糊搜索）、`status` 可选（0 停用、1 启用）、`page`、`limit` | 分页；每项 `{id,name,email,status}` |
| `GET /admin/getAdminInfo` | query：`id` 必填 | `{id,name,email}` |
| `POST /admin/editAdmin` | `id`、`name`、`email` 必填；`password` 可选，若传则遵守密码规则 | `[]` |
| `POST /admin/delAdmin` | `id` 必填 | `[]` |
| `POST /admin/editAdminStatus` | `id`、`status` 必填；`status` 使用 0/1 | `[]` |

### 7.2 管理员操作日志

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getOperationLogList` | query：`page`、`limit` 可选，`limit` 1～100 | 分页 `AdminOperationLog` |

`AdminOperationLog`：

```json
{
  "id": 1,
  "admin_id": 1,
  "admin_name": "Admin",
  "admin_email": "admin@example.com",
  "module": "withdrawal",
  "action": "review",
  "content": "审核出金订单 ...",
  "target_type": "withdrawal",
  "target_id": "1",
  "ip": "127.0.0.1",
  "operated_at": "2026-08-14 10:00"
}
```

第一版只记录管理员操作，不提供筛选、详情和导出接口。

### 7.3 运营总览

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getOperationOverview` | 无 | `OperationOverview` |

页面进入时调用一次本接口即可。正常代理、三币种账面余额、待处理业务、近7日交易趋势和最近5笔交易均由后端按统一口径聚合；前端不要再分别拉取业务分页列表自行统计。总览的近期交易只允许查看详情，业务操作仍进入入金、兑换、白名单或USD出金模块处理。

本接口已于2026-08-18通过用户ApiPost测试。前端以当前 `OperationOverview` 返回结构为接入基线，不再使用 `/admin/getTransactionList?limit=5` 代替运营总览，也不自行汇总7日趋势或待处理数量。

### 7.4 代理账户管理

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `POST /admin/addUser` | `company_name` 必填字符串 ≤255；`email` 必填合法邮箱 ≤191；`phone` 必填字符串 ≤50 | `AgentAccount`，另有 `invitation: MailResult` |
| `GET /admin/getUserList` | query：`keyword` 可选（编号/公司/邮箱/电话）、`status` 可选 0～3、`page`、`limit` | 分页 `AgentAccount` |
| `GET /admin/getUserInfo` | query：`id` 必填正整数 | `AgentAccount`；当前不返回登录日志和邮件日志 |
| `POST /admin/editUser` | `id`、`company_name`、`email`、`phone` 必填；长度同新增 | `AgentAccount`，另有 `mail_notifications: MailResult[]` |
| `POST /admin/editUserStatus` | `id` 必填；`status` 必填且为 1/2/3 | 更新后的 `AgentAccount`；必须符合第 4.1 节状态流转 |
| `POST /admin/resendUserInvitation` | `id` 必填；仅状态 0 待激活代理可用 | `MailResult` |
| `POST /admin/resetUserPassword` | `id` 必填；仅状态 1 正常或 2 暂停代理可用 | `MailResult` |

代理邮箱发生变化时旧登录 Token 失效。停用代理时 Token 失效。

### 7.5 代理资产

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getAgentAssetList` | query：`user_id` 可选；`keyword` 可选（编号/公司/邮箱）；`status` 可选 0～3；`page`、`limit` | 分页 `AgentAssets` |
| `GET /admin/getAgentAssetOverview` | query：`user_id` 必填正整数 | `AssetOverview` |

### 7.6 币种管理与业务配置

#### 币种网络和平台收款地址

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getCurrencyNetworkList` | query：`keyword` 可选 ≤100；`status` 可选 0/1；`page`、`limit` | 分页 `CurrencyNetwork` |
| `GET /admin/getCurrencyNetworkInfo` | query：`id` 必填正整数 | `CurrencyNetwork`，额外返回 `receiving_addresses` 历史地址数组 |
| `POST /admin/editCurrencyNetworkStatus` | `id` 必填；`status` 必填 0/1 | 更新后的 `CurrencyNetwork`；启用时必须已有当前有效地址 |
| `POST /admin/setReceivingAddress` | `currency_network_id` 必填；`address` 必填、≤255 且不能含空白；`remark` 可选 ≤500 | 新的当前 `ReceivingAddress` |

`CurrencyNetwork`：

```json
{
  "id": 1,
  "currency": {"id": 1, "code": "USDT", "name": "Tether", "type": 1, "status": 1},
  "network": {"id": 1, "code": "TRC20", "name": "TRON", "status": 1},
  "status": 1,
  "status_name": "启用",
  "is_effective": true,
  "current_receiving_address": {
    "id": 1,
    "address": "...",
    "remark": null,
    "status": 1,
    "activated_at": "2026-08-14 10:00"
  },
  "created_at": "2026-08-14 10:00",
  "updated_at": "2026-08-14 10:00"
}
```

`ReceivingAddress`：

```json
{
  "id": 1,
  "currency_network_id": 1,
  "currency": {},
  "network": {},
  "address": "...",
  "remark": null,
  "status": 1,
  "status_name": "启用",
  "is_effective": true,
  "activated_at": "2026-08-14 10:00",
  "deactivated_at": null,
  "created_at": "2026-08-14 10:00",
  "updated_at": "2026-08-14 10:00"
}
```

设置新地址会停用旧地址但保留历史记录。

#### 兑换比例和 USD 出金手续费

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getRateFeeConfig` | 无 | `RateFeeConfig` |
| `POST /admin/setDefaultExchangeRates` | `usdt_rate`、`usdc_rate` 必填十进制字符串，整数 ≤16 位、小数 ≤12 位，且必须大于 0 | 更新后的 `RateFeeConfig` |
| `GET /admin/getAgentExchangeRateList` | query：`keyword` 可选 ≤100；`status` 可选 0～3；`page`、`limit` | 分页 `AgentRateConfig` |
| `POST /admin/setAgentExchangeRates` | `user_id` 必填；`usdt_rate`、`usdc_rate` 格式同默认比例且必须大于 0 | 更新后的 `AgentRateConfig` |
| `POST /admin/clearAgentExchangeRates` | `user_id` 必填 | 清除后使用默认比例的 `AgentRateConfig` |
| `POST /admin/setUsdWithdrawalFee` | `fee_amount` 必填十进制字符串，整数 ≤20 位、小数 ≤8 位；允许 0 | 更新后的 `WithdrawalFeeConfig` |

`RateFeeConfig`：

```json
{
  "default_exchange_rates": {
    "USDT": {
      "id": 1,
      "source_currency": {},
      "target_currency": {},
      "rate": "1.000000000000",
      "updated_at": "2026-08-14 10:00"
    },
    "USDC": {}
  },
  "usd_withdrawal_fee": {
    "id": 1,
    "currency": {},
    "fee_amount": "10.00000000",
    "updated_at": "2026-08-14 10:00"
  }
}
```

`AgentRateConfig`：

```json
{
  "user_id": 1,
  "agent_code": "AG00000001",
  "company_name": "Example Ltd",
  "email": "agent@example.com",
  "status": 1,
  "status_name": "正常",
  "has_custom_rate": true,
  "rates": {
    "USDT": {
      "source_currency": {},
      "target_currency": {},
      "default_rate": "1.000000000000",
      "custom_rate": "0.990000000000",
      "effective_rate": "0.990000000000",
      "rate_source": "agent",
      "rate_source_name": "代理专属"
    },
    "USDC": {}
  }
}
```

### 7.7 入金管理

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getDepositList` | query：`user_id`、`currency_id`、`network_id` 可选正整数；`status` 可选 0/1/2；`keyword` ≤191；`order_no` ≤32；`txid` ≤255；`started_at`、`ended_at`；`page`、`limit` | 分页 `DepositOrder` 列表字段 |
| `GET /admin/getDepositInfo` | query：`id` 必填正整数 | `DepositOrder` 详情 |
| `POST /admin/reviewDeposit` | `id` 必填；`decision` 为 `approve`/`reject`；驳回时 `review_note` 必填且 ≤1000 | 审核后的 `DepositOrder` 详情 |

只有待审核订单可审核。通过后立即增加对应币种可用余额，不能重复审核。

### 7.8 兑换管理

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getExchangeList` | query：`user_id`、`source_currency_id` 可选正整数；`status` 可选 0/1/2；`keyword` ≤191；`order_no` ≤32；日期；`page`、`limit` | 分页 `ExchangeOrder` 列表字段 |
| `GET /admin/getExchangeInfo` | query：`id` 必填正整数 | `ExchangeOrder` 详情 |
| `POST /admin/reviewExchange` | `id` 必填；`decision` 为 `approve`/`reject`；驳回时 `review_note` 必填且 ≤1000 | 审核后的 `ExchangeOrder` 详情 |

### 7.9 统一交易记录

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getTransactionList` | query：`user_id`；`business_type=deposit/exchange/withdrawal`；`status_group`；`currency_code=USDT/USDC/USD`；`order_no` ≤32；`keyword` ≤191；日期；`page`、`limit` | 分页 `TransactionItem` |
| `GET /admin/getTransactionInfo` | query：`business_type` 必填；`business_id` 必填正整数 | `{transaction: TransactionItem, detail: 业务详情}` |

管理端的 `keyword` 会匹配订单号、代理编号、公司名称和邮箱。打开详情时使用列表返回的 `detail_type/detail_id`，分别传给 `business_type/business_id`。

管理端运营总览不直接调用本列表接口，而是调用 `GET /admin/getOperationOverview`，由该聚合接口同时返回近7日趋势和最近5笔交易。完整交易记录页面仍使用本节两个接口。

交易记录列表和详情只允许“查看详情”。审核入金、审核兑换、出金补件、出金审核和付款处理必须进入对应管理模块并调用对应业务接口。

### 7.10 白名单管理

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getWhitelistList` | query：`page`、`limit` | 分页 `WhitelistItem` 列表字段；第一版无业务筛选 |
| `GET /admin/getWhitelistInfo` | query：`id` 必填正整数 | `WhitelistItem` 详情 |
| `POST /admin/requestWhitelistSupplement` | `id` 必填；`message` 必填字符串 | 更新后的 `WhitelistItem` 详情；仅待审核可要求补件 |
| `POST /admin/reviewWhitelist` | `id` 必填；`decision=approve/reject`；驳回时 `review_note` 必填 | 更新后的 `WhitelistItem` 详情；通过只允许状态 0 待审核，驳回允许状态 0 待审核或 1 待补件 |
| `GET /admin/previewWhitelistFile` | query：`file_id` 必填正整数 | 文件二进制 inline |
| `GET /admin/downloadWhitelistFile` | query：`file_id` 必填正整数 | 文件二进制 attachment |

### 7.11 USD 出金管理

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /admin/getWithdrawalList` | query：`user_id`；`status` 0～5；`keyword`；`order_no`；日期；`page`、`limit` | 分页 `WithdrawalOrder` 列表字段 |
| `GET /admin/getWithdrawalInfo` | query：`id` 必填正整数 | `WithdrawalOrder` 详情 |
| `POST /admin/requestWithdrawalSupplement` | `id`、`message` 必填 | 更新后的详情；仅待审核可要求补件 |
| `POST /admin/reviewWithdrawal` | `id` 必填；`decision=approve/reject`；驳回时 `review_note` 必填 | 更新后的详情；通过只允许状态 0，完成后进入付款处理中；驳回允许状态 0/1，并释放冻结资金 |
| `POST /admin/uploadWithdrawalFile` | multipart：`file` 必填；PDF/PNG/JPG/JPEG，最大 10 MB | 未绑定的付款凭证文件对象 |
| `POST /admin/processWithdrawalPayment` | `id`；`result=complete/fail`；`file_ids` 可选数组，最多 5 个；失败时 `failure_reason` 必填 | 更新后的详情；仅付款处理中可提交；`result=fail` 时不能传 `file_ids` |
| `POST /admin/appendWithdrawalPaymentFiles` | `id`；`file_ids` 必填数组 1～5 个；`message` 可选 | 更新后的详情；仅已完成订单可追加付款凭证 |
| `GET /admin/previewWithdrawalFile` | query：`file_id` 必填 | 文件二进制 inline |
| `GET /admin/downloadWithdrawalFile` | query：`file_id` 必填 | 文件二进制 attachment |

付款完成推荐调用顺序：

1. 如需凭证，逐个调用 `/admin/uploadWithdrawalFile`，收集响应中的 `file_id`；不上传也允许完成。
2. 调用 `/admin/processWithdrawalPayment`，传 `result: "complete"` 和可选 `file_ids`。
3. 订单完成后如需补充凭证，再上传文件并调用 `/admin/appendWithdrawalPaymentFiles`。

付款失败调用 `/admin/processWithdrawalPayment`，传 `result: "fail"` 和 `failure_reason`。返回状态为 `5` 付款失败后，前端不得显示原订单重试入口；如需继续出金，代理必须重新提交新订单。

## 8. 代理端接口

`/web/agentLogin`、激活和找回/重置密码无需 Token；其余接口必须携带代理端 Token。代理端业务接口永远根据 Token 确定代理身份，不传 `user_id`。

### 8.1 账户认证

#### `POST /web/agentLogin`

- 鉴权：无。
- 参数：登录加密信封，见第 3 节；信封明文为 `email`、`password`。
- 成功返回：

```json
{
  "id": 1,
  "agent_code": "AG00000001",
  "company_name": "Example Ltd",
  "email": "agent@example.com",
  "phone": "+85212345678",
  "status": 1,
  "status_name": "正常",
  "activated_at": "2026-08-14 10:00",
  "last_login_at": "2026-08-14 12:00",
  "portal": "agent",
  "token_version": 1,
  "token": "代理端 Token"
}
```

只有状态 1 正常代理可以登录。连续密码错误可能触发临时锁定。

#### 其他认证接口

密码统一要求：8～20 位，只允许大小写字母和数字，并且至少包含一个大写字母、一个小写字母和一个数字。

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `POST /web/activateAgent` | `token` 必填 64 字符；`password`、`password_confirmation` 必填且一致 | 激活后的代理 Profile |
| `POST /web/forgotAgentPassword` | `email` 必填合法邮箱 ≤191 | `[]`；无论邮箱是否存在都返回同样成功结果 |
| `POST /web/resetAgentPassword` | `token` 必填 64 字符；新密码和确认密码 | 重置后的代理 Profile；全部旧登录 Token 失效 |
| `POST /web/agentLogout` | 无 | `[]`；当前 Token 加入黑名单 |
| `GET /web/getAgentProfile` | 无 | 代理 Profile |
| `POST /web/updateAgentPassword` | `current_password` 必填 ≤255；新密码和确认密码 | `[]`；成功后当前和其他旧 Token 失效，前端跳转登录页 |

### 8.2 资产和业务总览

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /web/getAgentAssets` | 无 | `AgentAssets` |
| `GET /web/getAgentAssetOverview` | 无 | `AssetOverview` |

业务总览的近期交易使用 `recent_orders`，应完整展示返回的 5 条记录，不使用水平滚动裁切关键信息。

### 8.3 数字货币入金

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /web/getDepositChannelList` | 无 | 数组；每项 `{currency_network_id,currency,network,receiving_address}` |
| `POST /web/submitDeposit` | `currency_network_id` 正整数；`amount` 十进制字符串，整数 ≤20 位、小数 ≤8 位且业务上必须 >0；`txid` 16～255 位且只能含字母数字 | 新建的 `DepositOrder` 详情 |
| `GET /web/getDepositList` | query：`currency_id`、`network_id`；`status` 0/1/2；`order_no`；`txid`；日期；`page`、`limit` | 当前代理的分页 `DepositOrder` 列表 |
| `GET /web/getDepositInfo` | query：`id` 必填正整数 | 当前代理的 `DepositOrder` 详情 |

入金通道项示例：

```json
{
  "currency_network_id": 1,
  "currency": {"id": 1, "code": "USDT", "name": "Tether", "decimal_places": 6},
  "network": {"id": 1, "code": "TRC20", "name": "TRON"},
  "receiving_address": {"id": 1, "address": "..."}
}
```

同一个币种和网络下的 `txid` 不能重复提交。提交成功并不代表已入账，需要管理员审核通过。

### 8.4 数字货币兑换 USD

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /web/getExchangeConfig` | 无 | `{balances: Balance[], rates: {USDT: EffectiveRate, USDC: EffectiveRate}}` |
| `POST /web/submitExchange` | `source_currency_code` 必填且为 `USDT`/`USDC`；`amount` 十进制字符串，整数 ≤20 位、小数 ≤8 位且业务上必须 >0 | 新建的 `ExchangeOrder` 详情 |
| `GET /web/getExchangeList` | query：`source_currency_id`；`status` 0/1/2；`order_no`；日期；`page`、`limit` | 当前代理的分页 `ExchangeOrder` 列表 |
| `GET /web/getExchangeInfo` | query：`id` 必填正整数 | 当前代理的 `ExchangeOrder` 详情 |

兑换配置中的 `Balance` 与 `AssetOverview.assets` 元素结构一致。提交时后端重新读取并保存实际比例快照，不能信任前端自己计算的目标金额。

### 8.5 统一交易记录

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /web/getTransactionList` | query：`business_type`；`status_group`；`currency_code`；`order_no`；日期；`page`、`limit` | 当前代理的分页 `TransactionItem` |
| `GET /web/getTransactionInfo` | query：`business_type` 必填；`business_id` 必填正整数 | `{transaction: TransactionItem, detail: 业务详情}` |

代理端不支持 `user_id` 和 `keyword`，也不能通过传入其他代理 ID 查看数据。

代理端交易记录详情同样只读。补交出金证明文件等操作必须回到USD出金业务页面，并调用对应出金接口。

### 8.6 白名单

#### 文件上传规则

- 支持 PDF、PNG、JPG、JPEG。
- 单文件最大 10 MB。
- 上传成功只生成临时未绑定文件，后续提交或补件时必须传它的 `file_id`。
- 每一轮最多绑定 5 个文件。
- 只能绑定当前代理自己上传且尚未绑定的文件。

#### 接口

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `POST /web/uploadWhitelistFile` | multipart：`file` 必填 | 白名单文件对象 |
| `POST /web/submitWhitelist` | `role`、`entity_type` 及对应业务字段；`file_ids` 可选数组，最多 5 个 | 新建的 `WhitelistItem` 详情 |
| `GET /web/getWhitelistList` | query：`page`、`limit` | 当前代理的分页 `WhitelistItem` 列表 |
| `GET /web/getWhitelistInfo` | query：`id` 必填 | 当前代理的 `WhitelistItem` 详情 |
| `POST /web/supplementWhitelist` | `id`；`file_ids` 必填数组 1～5 个；`message` 可选 | 补件后的详情；仅待补件状态可用 |
| `GET /web/previewWhitelistFile` | query：`file_id` 必填 | 当前代理有权访问的文件二进制 inline |
| `GET /web/downloadWhitelistFile` | query：`file_id` 必填 | 当前代理有权访问的文件二进制 attachment |

#### 提交白名单的条件字段

公共字段：

- `role`：必填，1/2。
- `entity_type`：必填，1/2。
- `file_ids`：可选数组，0～5 个正整数，不能重复。

付款人公司（`role=1, entity_type=1`）：

- `company_name`
- `registration_country`
- `operating_country`
- `city`
- `address`
- `registration_date`
- `company_type`：1/2
- `document_no`

付款人个人（`role=1, entity_type=2`）：

- `given_name`
- `surname`
- `nationality`
- `residence_country`
- `city`
- `address`
- `birth_date`
- `document_type`：1/2
- `document_no`

收款人公司（`role=2, entity_type=1`）：

- `company_name`
- `operating_country`
- `city`
- `address`
- `bank_name`
- `bank_account`
- `swift`
- `intermediary_swift` 可选
- `remittance_purpose` 必填正整数
- `remark` 可选

收款人个人（`role=2, entity_type=2`）：

- `given_name`
- `surname`
- `nationality`
- `residence_country`
- `city`
- `address`
- `bank_name`
- `bank_account`
- `swift`
- `intermediary_swift` 可选
- `remittance_purpose` 必填正整数
- `remark` 可选

### 8.7 USD 出金

#### 接口

| 接口 | 参数 | `data` 返回 |
|---|---|---|
| `GET /web/getWithdrawalConfig` | 无 | USD 余额、固定手续费、已通过的付款人/收款人和文件规则 |
| `POST /web/uploadWithdrawalFile` | multipart：`file` 必填；PDF/PNG/JPG/JPEG，最大 10 MB | 未绑定的代理证明文件对象 |
| `POST /web/submitWithdrawal` | `amount` 十进制字符串；`payer_whitelist_id`、`payee_whitelist_id`；`file_ids` 可选数组最多 5 个 | 新建的 `WithdrawalOrder` 详情 |
| `GET /web/getWithdrawalList` | query：`status` 0～5；`order_no`；日期；`page`、`limit` | 当前代理的分页 `WithdrawalOrder` 列表 |
| `GET /web/getWithdrawalInfo` | query：`id` 必填 | 当前代理的 `WithdrawalOrder` 详情，包括管理员付款凭证 |
| `POST /web/supplementWithdrawal` | `id`；`file_ids` 必填数组 1～5 个；`message` 可选 | 补件后的详情；仅待补件状态可用 |
| `GET /web/previewWithdrawalFile` | query：`file_id` 必填 | 当前代理有权访问的文件二进制 inline |
| `GET /web/downloadWithdrawalFile` | query：`file_id` 必填 | 当前代理有权访问的文件二进制 attachment |

`GET /web/getWithdrawalConfig` 返回结构：

```json
{
  "currency": {"id": 3, "code": "USD", "name": "US Dollar", "decimal_places": 2},
  "balance": {
    "available_balance": "1000.00",
    "frozen_balance": "0.00",
    "total_balance": "1000.00"
  },
  "fee_amount": "10.00",
  "payers": [],
  "payees": [],
  "file_rules": {
    "initial_required": false,
    "max_files_per_round": 5,
    "max_file_size_mb": 10,
    "allowed_extensions": ["pdf", "png", "jpg", "jpeg"],
    "unbound_expire_hours": 24
  }
}
```

付款人/收款人选项结构：

```json
{
  "id": 1,
  "whitelist_no": "...",
  "role": 1,
  "entity_type": 1,
  "entity_type_name": "公司",
  "subject_name": "Example Ltd",
  "business_data": {}
}
```

提交出金推荐调用顺序：

1. 先调用 `/web/getWithdrawalConfig` 获取 USD 可用余额、手续费及可选白名单。
2. 如代理选择上传证明文件，逐个调用 `/web/uploadWithdrawalFile` 并收集 `file_id`；不上传也可以继续。
3. 提交 `/web/submitWithdrawal`。前端只传 `amount`，手续费和总扣款由后端配置计算。
4. 后端校验 `可用余额 >= amount + fee_amount`，成功后冻结总扣款。
5. 管理员要求补件后，代理先上传新文件，再调用 `/web/supplementWithdrawal`。

## 9. 前端 AI 调用示例

### 9.1 普通 GET 列表

```js
const response = await http.get('/web/getTransactionList', {
  params: {
    page: 1,
    limit: 15,
    business_type: 'withdrawal'
  },
  headers: {
    Authorization: `Bearer ${agentToken}`
  }
})

if (Number(response.data.status) !== 200) {
  throw new Error(response.data.message)
}

const rows = response.data.data.data
const total = response.data.data.total
```

### 9.2 普通 POST

```js
const response = await http.post('/web/submitExchange', {
  source_currency_code: 'USDT',
  amount: '100.00000000'
}, {
  headers: {
    Authorization: `Bearer ${agentToken}`
  }
})
```

### 9.3 上传并绑定文件

```js
const form = new FormData()
form.append('file', selectedFile)

const uploadResponse = await http.post('/web/uploadWithdrawalFile', form, {
  headers: {
    Authorization: `Bearer ${agentToken}`
  }
})

const fileId = uploadResponse.data.data.file_id

await http.post('/web/submitWithdrawal', {
  amount: '100.00',
  payer_whitelist_id: 1,
  payee_whitelist_id: 2,
  file_ids: [fileId]
}, {
  headers: {
    Authorization: `Bearer ${agentToken}`
  }
})
```

### 9.4 带 Token 预览私有文件

```js
const response = await http.get('/web/previewWithdrawalFile', {
  params: { file_id: 1 },
  responseType: 'blob',
  headers: {
    Authorization: `Bearer ${agentToken}`
  }
})

const objectUrl = URL.createObjectURL(response.data)
// 将 objectUrl 用于 iframe、img 或新窗口；使用完后 URL.revokeObjectURL(objectUrl)
```

## 10. 前端实现检查清单

- [ ] 管理端与代理端使用独立 Token 存储键。
- [ ] 每次请求都检查响应头的新 `Authorization` 并刷新本地 Token。
- [ ] 登录严格按一次性公钥协议实现，公钥过期时重新获取。
- [ ] 金额和比例始终保留字符串，不进行浮点计算。
- [ ] 分页从 `data.data` 读取列表，从 `data.total` 读取总数。
- [ ] 独立交易记录页调用 Transaction 接口；代理总览读取 `recent_orders`；管理端运营总览调用一次 `getOperationOverview`；具体业务页继续调用各业务接口。
- [ ] 余额变动审计没有前端查询接口，也不根据交易状态推算余额变化。
- [ ] 代理端不传 `user_id`，管理端需要指定代理时才传 `user_id`。
- [ ] 文件上传后保存 `file_id`，提交/补件/追加凭证时再绑定。
- [ ] 私有文件使用带 Token 的 Blob 请求预览或下载。
- [ ] USD 出金初次证明文件非必填。
- [ ] 付款完成时管理员凭证非必填，完成后允许追加。
- [ ] 付款失败订单不显示重试操作。
- [ ] 具体业务页按状态和产品规则显示操作按钮，不依赖当前未返回的 `available_actions`。
- [ ] 管理端和代理端的交易记录详情始终只读，不显示审核、补件、付款或追加凭证按钮。
