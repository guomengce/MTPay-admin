# MTPay 前端接口调用规范

> 适用范围：`MTPay-admin` 管理端与 `MTPay-web` 代理端。
>
> 使用要求：每次开始接入一个新业务模块前，必须先阅读本文、对应项目内的接口文档以及该模块现有代码。本文负责约束代码组织和接入流程，接口路径、参数、状态和返回字段以对应项目的接口文档为准。

## 1. 核心原则

1. 一个业务模块只能保留一套真实数据链路。真实接口接入后，必须删除该模块的假数据、假接口、占位类型、前端本地分页、`console.log` 提交和失败后回退假数据的逻辑。
2. 接口函数统一写在 `src/api/modules/`。复杂业务模块的数据状态和业务操作放在 `composables/`；单页面、单流程的简单功能允许在 `index.vue` 中直接调用 API 函数。
3. 是否创建 composable 必须根据页面复杂度判断，禁止为了形式统一而机械拆分。
4. 筛选栏、表格、卡片、表单弹框、详情弹框等独立视觉或交互单元应拆成组件。
5. 复杂模块的 composable 按功能拆分，不把所有逻辑堆进一个文件；需要多个 composable 时使用组合入口统一提供给页面。
6. 前端不得根据页面假数据猜测后端字段、状态和接口路径。必须以接口文档为准。
7. 金额、余额、手续费和兑换比例始终使用字符串，不得转换为 JavaScript `Number` 参与资金计算。
8. 所有新增或修改的接口函数、复杂业务函数都必须写备注，说明用途、参数来源、状态限制和重要副作用。

## 2. 标准目录结构

包含列表、筛选、详情和增删改查的复杂业务模块建议采用以下结构：

```text
src/
├─ api/
│  └─ modules/
│     └─ agent.ts                  # 接口路径、参数类型、返回类型
└─ views/
   └─ agent/
      ├─ index.vue                 # 只负责页面编排
      ├─ components/
      │  ├─ AgentFilters.vue       # 筛选区域
      │  ├─ AgentTableList.vue     # PC 表格
      │  ├─ AgentCardList.vue      # 移动端卡片
      │  ├─ AgentFormDialog.vue    # 新增/修改弹框
      │  └─ AgentDetailDialog.vue  # 详情弹框
      └─ composables/
         ├─ useAgentList.ts        # 列表、分页、筛选
         ├─ useAgentForm.ts        # 新增、修改
         ├─ useAgentDetail.ts      # 详情
         ├─ useAgentStatus.ts      # 状态操作
         ├─ useAgentMail.ts        # 邮件操作
         └─ useAgentManagement.ts  # 组合入口
```

文件数量根据模块实际能力调整。没有邮件或状态操作的模块不需要创建对应文件；简单页面可以完全不创建 `components/` 和 `composables/`。

### 2.1 什么时候不需要 composable

满足以下大部分条件时，允许直接在页面 `index.vue` 中调用 API 模块导出的接口函数：

- 单页面、单表单或单一操作流程；
- 没有列表、分页和复杂筛选；
- 没有多个弹框或多个子组件需要共享状态；
- 请求状态只服务当前页面；
- 页面逻辑较短，拆分后反而需要频繁跳转文件阅读。

典型示例：

- 代理端账户激活页；
- 代理端邮件 Token 重置密码页；
- 只有一个提交按钮的简单配置页。

这类页面仍然必须把接口函数写在 `src/api/modules/*.ts`，页面只负责表单校验、Loading、调用接口和成功后的页面跳转，不得直接使用原始 `request.get/post`。

### 2.2 什么时候必须使用 composables

出现以下任一情况时，应使用 composable；出现多种情况时，应按功能拆成多个 composable：

- 列表、分页、搜索和多个筛选条件；
- 新增、详情、修改、删除、审核或状态流转；
- 多个弹框、表格、移动端卡片等组件协作；
- 文件上传、绑定、预览、下载等多步骤流程；
- 多个组件共享 Loading、选中项或业务状态；
- 页面逻辑已经明显影响 `index.vue` 的可读性。

## 3. 各层职责

### 3.1 `src/api/modules/*.ts`

API 文件只负责：

- 后端原始请求参数类型；
- 后端原始返回对象类型；
- 接口函数和真实接口路径；
- 接口用途、鉴权、状态限制及副作用备注。

API 文件不得包含：

- 页面弹框状态；
- Element Plus 消息提示；
- 二次确认框；
- 页面字段格式化；
- 假数据或接口失败回退数据。

标准写法：

```ts
import request from '../request';

/** 后端代理账户原始对象，字段名与接口返回保持一致。 */
export interface AgentAccount {
  id: number;
  agent_code: string;
  company_name: string;
  status: 0 | 1 | 2 | 3;
  status_name: string;
}

/** 获取代理账户分页列表，可按关键字和状态筛选。 */
export function fetchAgentList(params: AgentListParams) {
  return request.get<unknown, AgentPageResult>('/admin/getUserList', { params });
}

/** 修改代理状态；目标状态和合法流转由接口文档及后端共同约束。 */
export function updateAgentStatus(id: number, status: 1 | 2 | 3) {
  return request.post<unknown, AgentAccount>('/admin/editUserStatus', { id, status });
}
```

要求：

- API 类型优先保留后端字段名，不在 API 层擅自改成页面字段名。
- GET 参数放入 `params`；普通 POST 使用 JSON；上传使用 `FormData`。
- 后端使用 POST 修改数据时，不得根据个人习惯改成 PUT/PATCH。
- 接口前缀必须使用文档真实路径：管理端 `/admin`，代理端 `/web`，公共工具 `/api`。
- 不得在业务接口前额外拼接 `/api`。

### 3.2 `composables/*.ts`

复杂模块中的 composable 负责：

- 调用 API；
- 列表、分页、筛选条件；
- Loading、Submitting 等请求状态；
- 新增、修改、详情和状态操作；
- 操作前确认及操作后提示；
- 成功后刷新列表或详情；
- 业务返回结果的判断和展示。

推荐按一个功能块一个文件拆分。例如：

```text
useDepositList.ts
useDepositDetail.ts
useDepositReview.ts
useDepositManagement.ts
```

组合入口写法：

```ts
export function useAgentManagement() {
  const list = useAgentList();
  const form = useAgentForm(list.loadAgents);
  const detail = useAgentDetail();
  const status = useAgentStatus(list.loadAgents);

  return { ...list, ...form, ...detail, ...status };
}
```

composable 内必须使用明显的功能备注，例如：

```ts
/** 代理列表：负责后端分页、关键字搜索、状态筛选及初始化加载。 */
export function useAgentList() {}

/** 修改前读取最新详情，避免直接使用可能过期的列表资料。 */
async function openEdit() {}
```

### 3.3 `components/*.vue`

组件负责：

- 展示传入的数据；
- 收集表单输入；
- 触发语义明确的事件；
- PC/移动端布局和交互。

可复用业务组件不得直接导入 `src/api/modules/*` 调用接口。组件通过 `props` 接收数据，通过 `emit` 通知页面或 composable 执行操作。简单功能的页面级 `index.vue` 不受此条限制。

筛选组件示例职责：

```vue
<AgentFilters
  v-model:keyword="keyword"
  v-model:status="status"
  @search="search"
  @reset="resetFilters"
/>
```

### 3.4 `index.vue`

复杂业务模块的 `index.vue` 只负责：

- 引入页面组件；
- 调用模块组合 composable；
- 把状态传给组件；
- 把组件事件绑定到 composable 方法。

标准写法：

```ts
const {
  list, loading, page, limit, total,
  detailVisible, detailData,
  loadList, openDetail, submitReview,
} = useDepositManagement();
```

复杂业务模块的 `index.vue` 中禁止出现：

- `request.get()`、`request.post()`；
- 从 API 模块导入具体接口函数；
- 大段请求参数组装；
- 后端返回数据判断；
- 假数据数组；
- 本地模拟分页。

简单页面允许从 `src/api/modules/*` 导入接口函数并调用，例如激活页和重置密码页；但接口路径、Axios 请求和响应拦截仍必须留在 API/请求层。

## 4. 统一请求与响应规范

### 4.1 接口基础地址

基础地址通过环境变量配置：

```env
VITE_API_BASE_URL=https://mtpay-api-uat.qlclient.com
```

代码不得硬编码不同环境的域名。

### 4.2 Token

- 管理端和代理端使用不同的 Token 存储键，严禁混用。
- 请求拦截器统一携带 `Authorization: Bearer <token>`。
- 任意 Token 接口响应头出现新的 `Authorization` 时，统一请求层必须覆盖本地旧 Token。
- 页面和业务 composable 不重复拼接 Authorization。
- 401 由统一请求层清除登录态并跳转登录页。

### 4.3 成功与失败判断

后端业务响应使用：

```json
{
  "status": 200,
  "message": "操作成功",
  "data": {},
  "is_popup": false
}
```

- 统一请求层使用 `Number(status) === 200` 判断业务成功。
- 不得继续使用旧占位结构的 `code === 0`。
- 失败时统一显示后端 `message`。
- 页面或 composable 接收到的是解包后的 `data`，不得再次写 `result.data.data`，除非接口的业务数据本身就是分页结构。

### 4.4 分页

Laravel 分页结构中：

- 列表：`result.data`
- 总数：`result.total`
- 当前页：`result.current_page`
- 每页数量：`result.per_page`

请求参数统一使用后端字段 `page` 和 `limit`。接入真实列表后必须删除 `useTablePager` 等本地分页逻辑。

### 4.5 金额与比例

- 所有金额、余额、手续费、汇率使用字符串。
- 展示时可以添加货币单位和千分位，但不得改变原始业务值。
- 提交真实金额时不得通过 JavaScript 浮点计算推导。
- 兑换目标金额、手续费和总扣款以接口返回或后端计算为准。

### 4.6 文件上传、预览与下载

- 上传使用 `FormData`，不要手工设置 multipart boundary。
- 上传成功保存后端 `file_id`，业务提交时再绑定。
- 私有文件预览/下载必须使用带 Token 的 Blob 请求。
- 不得直接把鉴权文件 URL 放入 `<img>`、`<iframe>` 或 `<a>`。
- Blob URL 使用完必须调用 `URL.revokeObjectURL()`。

## 5. 标准接入流程

每接入一个模块，严格按以下顺序执行。

### 第一步：阅读和核对

1. 阅读本文。
2. 阅读当前项目内的接口文档。
3. 找到该模块的接口、参数、对象、枚举和状态流转。
4. 检查现有页面、组件、API 占位文件和 composable。
5. 列出真实接口与现有假字段的差异。

### 第二步：定义 API

1. 在 `src/api/modules/<module>.ts` 定义后端原始类型。
2. 写入真实路径和请求方式。
3. 为每个接口写清晰备注。
4. 删除该模块旧的假路径、假参数和重复接口函数。

### 第三步：判断复杂度并拆分组件

1. 先按第 2.1、2.2 节判断是简单页面还是复杂模块。
2. 简单页面可以只保留 `index.vue`，不要为了形式创建无意义组件。
3. 复杂模块的筛选区域独立为 `<Module>Filters.vue`。
4. PC 表格和移动端卡片按项目现有结构维护。
5. 新增/修改使用表单弹框组件，详情使用独立弹框或详情页面。
6. 可复用业务组件只接收数据和触发事件。

### 第四步：按需要编写 composables

1. 简单页面不强制创建 composable，可在 `index.vue` 调用 API 函数。
2. 复杂模块的列表、详情、表单、审核/状态、文件/邮件按功能拆文件。
3. 每个 composable 顶部写职责说明，复杂方法写副作用和状态限制备注。
4. 多个 composable 使用组合入口向 `index.vue` 暴露状态和方法。
5. 数据更新成功后刷新对应列表或详情。

### 第五步：清理旧内容

必须搜索并删除：

- 页面中的静态业务数组；
- `Promise.resolve`、`setTimeout` 模拟请求；
- `/agent/list` 等假路径；
- `console.log('submit...')`；
- 与真实接口无关的表单字段；
- 本地分页；
- 未使用的旧 composable 和类型；
- 真实接口失败后回退假数据的代码。

### 第六步：验证

1. 对当前模块运行 TypeScript 检查。
2. 运行 Vite 构建。
3. 复杂模块确认 `index.vue` 没有 API 调用；简单页面确认只调用 API 模块导出的函数，没有直接使用 request/Axios。
4. 搜索确认没有旧假接口和假数据。
5. 有真实账号和 Token 时，按列表、详情、新增、修改、状态/审核顺序测试。
6. 文件模块额外测试上传、绑定、预览、下载和错误响应。

## 6. 状态和操作按钮

- 页面操作按钮优先依据后端状态和 `available_actions` 显示。
- 没有 `available_actions` 时，严格按接口文档的状态流转显示。
- 禁止显示后端当前状态不允许执行的操作。
- 高风险操作必须二次确认，并在提示中显示业务主体、目标状态或目标邮箱。
- 状态修改、审核、付款等接口成功后必须刷新列表或详情。
- 不允许只在前端修改行数据来假装接口已成功。

## 7. 备注规范

接口函数备注至少说明“做什么”，必要时补充“什么状态可用”和“会产生什么副作用”：

```ts
/** 重新发送代理激活邮件；仅待激活状态可用。 */
export function resendAgentInvitation(id: number) {}

/**
 * 完成 USD 付款。
 * 完成后正式扣除冻结资金；付款失败时释放冻结资金且原订单不可重试。
 */
export function processWithdrawalPayment(payload: PaymentPayload) {}
```

不要写无信息量的备注：

```ts
/** 请求接口 */
/** 获取数据 */
/** 提交 */
```

## 8. 禁止事项

- 禁止可复用业务组件直接调用接口；简单页面级 `index.vue` 可以调用 API 模块导出的接口函数。
- 禁止一个模块同时保留新旧两套接口。
- 禁止为了适配旧页面而伪造后端不存在的字段。
- 禁止把管理端 Token 用于代理端，或反向混用。
- 禁止自行修改后端状态码、枚举含义和状态流转。
- 禁止使用浮点数处理资金。
- 禁止将交易记录接口和资金流水接口混用。
- 禁止未经确认新增 CSV 导出、统一审核中心等第一版未实现接口。
- 禁止登录加密公钥复用；每次登录重新获取一次性公钥。
- 禁止为激活、重置密码等公开接口错误添加登录鉴权。

## 9. AI 完成模块后的报告要求

完成后必须明确报告：

1. 已接入的真实接口路径。
2. 新增或修改的页面、组件与 composable；若简单页面未使用 composable，应说明复杂度判断。
3. 删除了哪些假数据、假接口和旧逻辑。
4. 状态流转或资金规则如何落地。
5. 类型检查和构建是否通过。
6. 尚未验证或需要真实后端数据确认的内容。

不得只回复“接口已接好”，也不得在未验证时声称真实联调全部通过。
